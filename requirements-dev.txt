# ---------------------------------------------------------------------------
# Development / CI dependencies.  Install with:
#   pip install -r requirements.txt -r requirements-dev.txt
# ---------------------------------------------------------------------------
-r requirements.txt

# --- Testing ----------------------------------------------------------------
pytest>=8.2,<9.0
pytest-asyncio>=0.23,<1.0
pytest-cov>=5.0,<6.0
respx>=0.21,<1.0               # mock httpx-based connector calls

# --- Quality gates ----------------------------------------------------------
ruff>=0.4,<1.0                 # lint + format
mypy>=1.10,<2.0
types-requests

# --- Tooling ----------------------------------------------------------------
pip-tools>=7.4,<8.0
pre-commit>=3.7,<4.0
