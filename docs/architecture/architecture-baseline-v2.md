# Architecture Baseline v2

| Attribute | Value |
| --- | --- |
| Document type | Architecture Baseline Index |
| Status | Living document — canonical answer to "what is locked" |
| Scope | Every architectural decision recorded 2026-07-24 in ADR-0031 through ADR-0039, plus the ADR-0020 status change and the ACT-001 closure those decisions produced |
| Source of truth | Each row's own ADR — this page is an index, not a governing document in its own right |
| Sibling documents | [Platform Capability Matrix](../governance/platform-capability-matrix.md) · [Architecture Freeze Index](../governance/architecture-freeze-index.md) · [Architecture Action Register](./architecture-action-register.md) · [Codebase Audit 2026-07-24](../audit/CODEBASE_AUDIT_2026-07-24.md) |

> This page **indexes** locked decisions; it does not **make** them. Each row's ADR is
> authoritative for its own decision. When a decision changes, its own ADR changes (or
> a new ADR supersedes it) and this index is updated to match — this index never drives
> a decision on its own.

---

## 1. Purpose

This is the single page a reader consults to answer "what is locked, and what is still open." It was created because nine architectural decisions were recorded in one sitting (2026-07-24), each with its own TBD items — without one index, a reader would have to open all nine ADRs to know what remains undecided.

## 2. The baseline

| Decision | ADR | Status | Summary | TBD it carries |
| --- | --- | --- | --- | --- |
| Authoritative Layer Model | [ADR-0031](../adr/0031-authoritative-layer-model.md) | Accepted (Layer 4 redefined by ADR-0040) | Locks the 7-layer model (L1 Requirement Intelligence → L2 Feature Engineering → L3 Automation Engineering → L4 Suite Quality Governance → L5 Test Execution → L6 Failure Intelligence & Self-Healing → L7 Governance Dashboard), sourced from JIRA/SonarQube/ZAP. Fully supersedes ADR-0020; ADR-0020's Continuous Learning/Prediction & Insights/Optimization/Autonomous Engineering/Organizational Intelligence layers are redesignated Layer 1 sub-capabilities — nothing deleted. **L4's Purpose is no longer "validates Layers 2 and 3 via SonarQube and Gherkin lint"** — Gherkin lint moved to Layer 2's own CP2 and SonarQube to Layer 3's own CP3 (ADR-0040); L4 is suite-level integration governance only. | ADR-0020's Layer 2.5 (Executable Specification Engineering, CAP-087) is **not** redesignated by this decision — its placement is explicitly unresolved (ADR-0031 §D4). |
| Control Point Model and Layer 4 Redefinition | [ADR-0040](../adr/0040-control-point-model-and-layer-4-redefinition.md) | Accepted | Amends ADR-0031 (additive, does not supersede). Each generating layer owns its own in-layer control point: L1→CP1 (requirement readiness, ADR-0011), L2→CP2 (Gherkin lint + feature governance), L3→CP3 (SonarQube on generated Java, `customqa:*` profile). Repair loops bounded at 2 LLM attempts, then human-in-the-loop. All control-point gates are deterministic only (lint results, coverage counts, tag presence, duplicate detection, compilation) — LLM-judged assessments (e.g. "Business readability"/"Step reusability" in the `/validate-feature` prompt) are advisory only, never gating (mirrors ADR-0016/ADR-0017's deterministic-judgement principle). Layer 4 redefined as suite-level integration governance: step-definition binding coverage, orphaned-glue detection, near-duplicate step detection, suite compilation, aggregate release decision. | CP2's and CP3's own rule catalogues — deferred to Layer 2's and Layer 3's own future LLDs. SonarQube invocation mechanism — deferred to the Layer 3 LLD and ADR-0039 (still Proposed). |
| Layer 1 Capability Freeze | [ADR-0032](../adr/0032-layer-1-capability-freeze.md) | Accepted | Freezes Layer 1 (incl. its redesignated sub-capabilities) at its current baseline — no new Layer 1 CAP number without a lifting ADR. Five carve-outs permitted: emitting `TestableRequirement`, integrating the run/stage state model, ADR-0033's package renames, bugfixes, tests. | None — the freeze and its lifting procedure are fully specified. |
| Naming Disambiguation and Package Renames | [ADR-0033](../adr/0033-naming-disambiguation-and-package-renames.md) | Accepted | Locks the mapping for the "Quality Governance" and "Execution" name collisions: `requirement_intelligence/quality_governance/` → `requirement_quality_governance/`; `requirement_intelligence/execution/` → `execution_package/`; top-level `quality_governance/` → `suite_quality_governance/`; top-level `execution/` → `test_execution/`. Artifact filenames unaffected. Accepted ADRs using old names are not edited. | The rename itself is not executed by this ADR — deferred to a future implementation task under ADR-0032 carve-out 3. |
| TestableRequirement — the Layer 1 → Layer 2 Contract | [ADR-0034](../adr/0034-testable-requirement-contract.md) | Accepted (field list resolved by ADR-0042) | `AnalysisResult` redesignated Layer-1-internal. `TestableRequirement`/`TestableRequirementSet` established as the sole frozen Layer 1 → Layer 2 contract: platform-assigned content-addressed `REQ-*` IDs with a `supersedes` lineage field, structured `AC-*` acceptance criteria (no free prose), a run-level `PASS`/`PASS_WITH_WARNINGS` gate (a `FAIL` run emits nothing), a `TestableRequirementSet` envelope with `contract_version`/`run_id`/provenance, and a versioned, schema-checked, compatibility-tested contract. | **Exact field list — resolved, see ADR-0042.** Still open: the `supersedes`-detection mechanism (ADR-0042 Decision 5 — blocked on a cross-run requirement index ADR-0036 does not provide). |
| Contract Consolidation and Dead Scaffolding Removal | [ADR-0035](../adr/0035-contract-consolidation-and-dead-scaffolding-removal.md) | Accepted (item 2 unblocked by ADR-0042) | Records four items for future removal (not executed by this ADR): the unused `SourceConnector` Protocol in `shared/contracts/base.py` (`Schema` in the same file explicitly preserved); `canonical_requirement.py` (unused, `SourceRef` a salvage candidate); `requirement_package.py` (unused, replaced by ADR-0034); the declared-but-unimported `jira` SDK dependency (`requirements.txt:27`). | ~~Whether `SourceRef` is salvaged into `TestableRequirement` before `canonical_requirement.py` is deleted~~ — **resolved by ADR-0042** (salvaged, actual `system`/`external_id`/`url` shape). Item 2's removal is unblocked; not yet executed. |
| TestableRequirement Field Specification | [ADR-0042](../adr/0042-testablerequirement-field-specification.md) | Accepted | Resolves ADR-0034's field-list TBDs: `TestableRequirement`/`TestableRequirementSet`/`AcceptanceCriterion`/`Risk`/`SourceRef` field lists; `REQ-*`/`AC-*`/`RSK-*` ID generation (shared normalization + sha256); `content_hash` (sha256 over canonical JSON excluding `requirement_id`/`content_hash`/`supersedes`); one `TestableRequirementSet` per run, unpartitioned; JSON Schema location established at `contracts/schemas/testable_requirement_set.schema.json` (no prior repo convention existed). `SourceRef` salvaged from `canonical_requirement.py` using its **actual** 3-field shape (`system`/`external_id`/`url`) — the task's proposed 4-field shape (with a `source_type` discriminator) did not match the real model and was not adopted. Layer 1 emits `risks`, never `riskBasedScenarios`; one requirement maps to exactly one `.feature` file; no `SCN-*` IDs cross this boundary (Layer 2 assigns them post-remediation). CAP-087/Layer 2.5 placement evaluated and left **open** (Decision 7) — its dependency boundary bypasses `TestableRequirementSet` and its output is renderer-agnostic, not Cucumber-committed; these are material differences from ADR-0031's Layer 2, not a naming collision. | `supersedes`-detection mechanism (Decision 5, blocked on a cross-run index). CAP-087/Layer 2.5 placement (Decision 7, sharpened not resolved — see §4 item 1). |
| Run and Stage State Model | [ADR-0036](../adr/0036-run-and-stage-state-model.md) | Accepted | `run_id` becomes the canonical run-directory identifier (`--execution-name` becomes an internal label). New `run_state.json` (additive to the unchanged `manifest.json`) tracks 19 stages (13 live Layer 1 stages + one reserved slot per Layer 2–7) through `PENDING`/`RUNNING`/`SUCCEEDED`/`FAILED`/`SKIPPED`. Resume from the first non-`SUCCEEDED` stage; stage inputs are artifact paths; stages must be idempotent; a lockfile prevents concurrent operation on one `run_id`. Filesystem only, no database. | `run_id` generation scheme; `run_state.json`'s exact schema; the lockfile's exact mechanism; whether Layer 7 (Governance Dashboard) genuinely participates as a per-run stage at all (ADR-0036 §D5 — structurally different from stages 1–18). |
| Generated Test Suite Target and SUT Binding | [ADR-0037](../adr/0037-generated-test-suite-target-and-sut-binding.md) | Accepted (Java-stack TBD resolved by ADR-0041; promotion TBD resolved by ADR-0045) | Generated suite lives in this repository: Java + Cucumber BDD (`.feature`, step definitions, page objects, test data). Two-tier split locked: TRACKED baseline (build config, framework, base page objects, the versioned `customqa:*` SonarQube profile as a Layer 4 asset) vs. UNTRACKED per-run workspace (where generated assets land; Layer 6 self-healing operates here only). Promotion workspace → baseline is explicit and reviewed for escalations, auto-promoted for clean validated non-duplicate assets (ADR-0045 narrows "never automatic"). SUT environment binding (URL/env/credentials) comes only from config/env files; the requirement source contributes only `SourceArtifact.component`/`SourceArtifact.location` (not a field called "endpoint" — verified against the model, no such field exists). | ~~Tracked baseline's exact repo path~~ — **resolved:** `test-suite-baseline/` at repo root (additive note in ADR-0037, 2026-07-25). Day-one base-framework shape (`BasePage`/`DriverFactory`/`Hooks`/`ConfigReader`) also recorded — realized, not frozen; extension expected as Layer 3 generates further code (additive note, 2026-07-25). ~~Exact promotion review mechanism~~ **resolved by [ADR-0045](../adr/0045-scoped-asset-promotion.md):** promotable gate + review model + capstone sequencing locked; the promotion mechanism's exact git mechanics remain TBD-at-implementation. (The concrete Java stack is resolved — see ADR-0041.) |
| Java Stack for the Generated Test Suite | [ADR-0041](../adr/0041-java-stack-for-the-generated-test-suite.md) | Accepted | Resolves ADR-0037's Java-stack TBD. Maven + Java 21 (`maven.compiler.release`); JUnit/Cucumber versions BOM-managed (`junit-bom:5.10.3`, `cucumber-bom:7.18.x`); JUnit Platform Suite runner (`@Suite`/`@IncludeEngines`/`@SelectClasspathResource`) — `@RunWith`/`@CucumberOptions` explicitly prohibited; JUnit Jupiter–only assertions (no AssertJ/Hamcrest/TestNG); Selenium 4.25.0 with Selenium Manager, no WebDriverManager, driver held in a `ThreadLocal` factory in the tracked baseline (page objects receive, never construct, a driver); three reporters (`message` NDJSON is Layer 6's sole machine-readable input, `junit` XML for Jenkins trends, `html` for humans); features at `src/test/resources/features/`, package root `com.automation`. Verified line-by-line against `docs/reference/automation-poc/pom.xml`; two decisions (BOM management, WebDriverManager exclusion) are recorded as deliberate divergences from that pom.xml's current state, not as mined matches. | None for this decision itself. Does not resolve ADR-0037's remaining TBDs (tracked baseline path, promotion review mechanism). |
| Layer 2 (Feature Engineering) Architecture Freeze | [ADR-0043](../adr/0043-layer-2-feature-engineering-architecture-freeze.md) | Accepted | Locks Layer 2's design against `TestableRequirementSet` (ADR-0034/0042) only — `AnalysisResult` is not a Layer 2 input. One requirement → one `.feature` file, deterministic, no LLM decomposition; risks→scenarios conversion lives only in Layer 2. Platform-assigned, content-addressed `SCN-*` ids, assigned post-remediation. Traceability as Gherkin tags (`@REQ-*` Feature-level, `@SCN-*`/`@AC-*` scenario-level, `@REQ-*` hoisted per `.gherkin-lintrc`'s `no-homogenous-tags` rule) feeding `messages.ndjson` (ADR-0041 D6) directly; `traceability.json` becomes a derived index. Gherkin lint ported to Python over the official Cucumber parser (17-rule `.gherkin-lintrc` preserved verbatim, not extended). Reuses the existing ADR-0014 prompt registry as a second consumer (future shared-service extraction flagged, not executed). CP2 gates deterministically only (ADR-0040): lint, AC coverage, tag presence, duplicates; 2-attempt bounded remediation; LLM-judged checks advisory only. A CP2 platform check (not a new lint rule) enforces a step-vocabulary index against near-duplicate steps. Layer 2 owns `Examples:` tables and a test-data *specification* — not Java, not a dataset (Layer 3's job). Occupies ADR-0036 stage 14, idempotent via `content_hash`. **Does not lift ADR-0032's freeze** — that freeze's precondition 1 (Layer 2 at Runtime Integration) is not met by an architecture-freeze ADR with zero implementation; Layer 2's own implementation is authorized independently of that freeze, which remains fully in force. Additive amendment notes added to ADR-0014 (second registry consumer) and ADR-0032 (freeze-lift finding). | CAP-087/Layer 2.5 placement (engaged, not resolved — ADR-0031 §D4/ADR-0042 Decision 7's two conflicts restated, unchanged). Prompt-registry extraction mechanism and its ADR-0032 carve-out fit (D4). Step-vocabulary index's storage/matching algorithm; test-data specification's exact field shape; the Python Gherkin-lint port's package structure; CP2's acceptance-criteria-coverage threshold. |
| Layer 3 (Automation Engineering) Architecture Freeze | [ADR-0044](../adr/0044-layer-3-automation-engineering-architecture-freeze.md) | Accepted (promotion TBD resolved by ADR-0045) | Locks Layer 3's design against the Validated Feature Package (ADR-0043) only. No Test Generator — `@RunWith(Cucumber.class)`/`@CucumberOptions` are prohibited (ADR-0041 D3) and the tracked `RunCucumberTest.java` already executes every feature; feature/tag selection at execution is Layer 5's. Reuse-first via a persistent, semantic asset catalog (Model B), reconciled from the tracked baseline at run start. A semantic reuse binding is trusted only when match confidence clears a threshold (else escalate), the catalogued code's content-hash validates, and the step-definition signature fits the Gherkin step — all three required, since a wrong binding is the one Layer 3 failure mode invisible to every downstream deterministic gate. CP3 gates deterministically on step/scenario coverage and duplicate/unmapped-step counts, reports reuse % without gating on it, and hard-gates on a live SonarQube quality-gate scan (specifying ADR-0040 Decision 1's own CP3/SonarQube assignment) — **corrected, 2026-08-05 (the stage-15 wiring build): the Sonar gate verifies GENERIC Java quality only** (the `Sonar way` baseline the `customqa` profile extends); both `customqa:*` rules (`direct-webdriver-action`, `long-method`) are static, `javalang`-based checks in Layer 3 itself, neither Sonar-gated (a Sonar-config discovery found `long-method`'s own backing rule, `java:S138`, permanently `scope:MAIN` and therefore structurally unable to reach this platform's `src/test/java`-resident generated code — see ADR-0044 D5's third revision note and item 22 below). CP3's composite is therefore seven criteria: four coverage + Sonar (generic) + two static customqa:* checks. `customqa:*` still feeds Layer 3's generation prompts as a constraint, unaffected by this correction. CP4 gates on static locator health from page-object source only — no running-browser/SUT dependency, unlike CP3's live-Sonar dependency. Test-data classes are generated from Layer 2's specification (ADR-0043 D7), never re-derived. Fires the `PromptCompatibility` generalization trigger (ADR-0043 §4 item 12(b)) without performing the generalization itself. Occupies ADR-0036 stage 15 — **wired (2026-08-05):** `automation_engineering/stage/runner.py` chains catalog/reuse/generation/CP3/CP4/promotion into a real, resumable stage, CLI-invocable via `analyze --with-automation-engineering` (off by default); stage 15 moved from a PENDING placeholder into `LIVE_STAGE_IDS` (item 20 below, resolved). Additive amendment notes added to ADR-0040 (CP3/SonarQube specification) and ADR-0043 (trigger fired). | Asset catalog's exact storage location; semantic-match mechanism (embeddings vs. per-step LLM call, lean embeddings); test-data specification → Java mapping. ~~The promotion mechanism (ADR-0037's own open TBD, now also gates catalog growth)~~ **resolved by [ADR-0045](../adr/0045-scoped-asset-promotion.md)**, pulled forward as Layer 3's own capstone build step; this ADR's own "does not own... the promotion mechanism" language (Consequences/Ownership) is superseded by ADR-0045's Acceptance, per that ADR's own reconciliation note. |
| Documentation Track Governance | [ADR-0038](../adr/0038-documentation-track-governance.md) | Accepted | Track A (`docs/adr/`, `docs/architecture/`, `docs/governance/`, `docs/proposals/`, `docs/reviews/`, `docs/releases/`) declared normative. Track B (`docs/product/`, `docs/handbook/`, `docs/standards/`) declared non-normative and frozen — kept, not maintained. Identifier collisions (`CAP-001` today) resolve by Track A precedence. Closes ACT-001 by precedence, not reconciliation (`docs/architecture/architecture-action-register.md` updated to Status: Closed, Verification Evidence: ADR-0038, in the same change). | None. |
| Execution Backend and CI/CD | [ADR-0039](../adr/0039-execution-backend-and-cicd.md) | **PROPOSED — NOT ACCEPTED** | Proposes Jenkins as the CI/CD tool, serving two roles that must not be conflated: (1) platform CI (ruff/mypy/pytest), (2) an execution backend for Layer 5 + the Layer 4 SonarQube scan of generated code — behind an `ExecutionBackend` interface, with a local Maven/Gradle runner as the default implementation so Layer 5 doesn't hard-depend on Jenkins. Flags that a Jenkins-delegated stage is asynchronous, making Layer 5 the pipeline's first non-synchronous stage under ADR-0036. `run_state.json` stays authoritative over Jenkins build history. | **The entire ADR is unratified** — nothing may be built against it until Accepted. Specifically open even once accepted: the async stage-execution mechanism, left to Layer 5's own future LLD. |
| Scoped Asset Promotion (Layer 3 Capstone, pulled from Layer 4) | [ADR-0045](../adr/0045-scoped-asset-promotion.md) | Accepted (git-mechanics TBD resolved by implementation, 2026-08-02; wrapping designed by ADR-0046) | Resolves ADR-0037's promotion TBD, pulled forward from a future Layer 4 build into Layer 3's own capstone because Layer 3's reuse catalog (ADR-0044 D3) cannot grow across runs without it. Locks the promotable gate (an asset auto-promotes when its feature passed CP2 and its automation passed CP3+CP4, and it is confirmed not a duplicate of an existing tracked-baseline asset — using ADR-0044 D3/D4's own content-hash/signature identity mechanism, no new duplicate detector). Locks the review model: clean, validated, non-duplicate assets auto-promote; anything ADR-0044 D4 escalates (low confidence, stale content-hash, signature mismatch) waits for that same human-in-the-loop review — one review queue, not two. Locks sequencing: promotion is Layer 3's last build step (catalog → reuse engine → generators → CP3 → CP4 → promotion). Locks the mechanism at the decision level only (copies validated assets from the workspace into the tracked baseline as a git-visible change); auto-commit vs. stage-for-review was TBD-at-implementation. Scopes out Layer 4's broader suite-level governance (orphaned-glue detection, suite-wide near-duplicate sweep, aggregate release gate) explicitly — those stay Layer 4's, which may later wrap this promotion decision in richer governance. Additive amendment notes added to ADR-0037 (promotion TBD resolved, "never automatic" narrowed), ADR-0040 (scope boundary with Layer 4 recorded), and ADR-0046 (the anticipated wrapping is now designed and locked). | ~~The promotion mechanism's exact git mechanics (auto-commit vs. stage-for-review)~~ **resolved by the 2026-08-02 promotion build (see §3 below): stage-for-review (`git add`, never `git commit`).** ~~Layer 4's wrapping governance~~ **designed by [ADR-0046](../adr/0046-layer-4-quality-governance-architecture-freeze.md).** |
| Layer 4 (Quality Governance) Architecture Freeze | [ADR-0046](../adr/0046-layer-4-quality-governance-architecture-freeze.md) | Accepted | Locks Layer 4's own CP5 (suite-integration governance, ADR-0040 Decision 3 / ADR-0044) in full: orphaned-glue detection (deterministic pattern-vs-literal-text matching as the primary, gating check; semantic-embedding similarity as an advisory-only secondary hint, never gating); the cross-suite near-duplicate sweep (step definitions only, matching both ADR-0040 Decision 3's own text and the reuse engine's existing embedding scope; reuses the embedding boundary's one-batched-call shape, O(1) API calls / O(n²) cheap in-memory cosine compute; default 0.90 cluster threshold, anchored to the real 0.8980 paraphrase measurement already on record, configurable); promotion-wrapping (a suite-level gate running after ADR-0045's per-asset promotion stages via `git add` and before commit, exploiting `reconcile()`'s working-tree read; a suite-reject routes to ADR-0045 D3's own shared review queue, never a second one); aggregate-release cohesion ("compiles" is Layer 4's, mirroring CP3's live-tool-not-live-SUT precedent; "runs" stays Layer 5's; ambiguous-glue v1 is exact pattern-collision across classes, deterministic). Locks CP5's own deterministic-gates/advisory-hints composition rule and an explicit reuse-not-rebuild list (the catalog, the embedding boundary, Gherkin-needs derivation, per-asset promotion — no second mechanism for any of them). Resolves the source deck's CP5/CP6 mislabeling (`docs/proposals/layer-4-quality-governance-lld.md` Finding 0): the deck's own "CP5" (whole-suite Sonar) and "CP6" (execution-readiness) are renumbered CP7 and CP8 — named and scoped only, detailed design deferred — since CP5/CP6 were already Accepted-claimed by ADR-0040/ADR-0044 for different content; the deck's dynamic-readiness half stays Layer 5's under no Layer 4 number. Governance reporting is Layer 4's; the dashboard stays Layer 7's (ADR-0031). Disambiguates the unrelated CAP-080 "Quality Governance" (ADR-0017) name collision without merging or renaming either subsystem. Additive amendment notes added to ADR-0040 (Decision 3 specified in full), ADR-0044 (its own CP5/CP6 statement confirmed and specified), and ADR-0045 (the anticipated Layer 4 wrapping now designed). | The orphan-detection pattern-vs-text evaluator (new build work, does not exist today). The near-dup cluster threshold — locked as a configurable default, not a proven constant; tune against real observed near-duplicate pairs once the catalog has some. `_cosine_similarity`/`_embedding_text`'s promotion to a shared location — locked requirement, not yet executed. Full Cucumber Expression/regex overlap ambiguous-glue detection — deferred beyond the exact-collision v1. ~~CP7's and CP8's own detailed design, rule catalogues, and effort estimates — deferred to a future design-then-freeze task; CP7's design must also verify Sonar Community Edition's actual capabilities before proceeding (LLD review's own S6 finding).~~ **resolved by [ADR-0047](../adr/0047-layer-4-cp7-cp8-freeze.md).** |
| Layer 4, CP7/CP8 Freeze | [ADR-0047](../adr/0047-layer-4-cp7-cp8-freeze.md) | Accepted | Locks CP7 (suite-wide Sonar governance, ADR-0046 D8) and CP8 (static execution-readiness, ADR-0046 D8) in full, resolving the eight open decisions `docs/proposals/layer-4-cp7-cp8-design.md` surfaced. Records the Sonar-CE discovery's own live-verified facts as the freeze's binding basis: this platform's real server is genuinely Community Edition; its one built-in quality gate is leak-period-scoped, the wrong tool for whole-suite governance; absolute measures are readable by the pipeline's own least-privilege token via `/api/measures/component`; `coverage`/`duplicated_lines_density` are unmeasured (no JaCoCo report ever submitted); Sonar CE genuinely evaluates security-hotspot/vulnerability rules for Java, only the review/triage workflow is permission-gated. CP7 reads absolute measures (never the built-in gate, never an admin-created custom one), inherits CP3's own `customqa:*`/Sonar honesty story unchanged, and ships entirely report-only — generic-quality/reliability rating-gating deferred until the suite genuinely compiles and real scores exist; security stays report-only pending an explicit future decision; coverage/duplication stay report-only pending a JaCoCo-wiring prerequisite. CP8 validates this platform's own real config (assets present via the reused catalog, `pom.xml` well-formed against its OWN declarations — never a hardcoded dependency list, `junit-platform.properties`'s `cucumber.glue` pointing at a real package, the tracked runner class present) and gates immediately, deterministically, unconditional on compile state — a distinct, complementary check from CP5-cohesion's own compile check (glue-package misconfiguration vs. compile failure), never merged into one verdict. Additive amendment notes added to ADR-0046 (D1/D8 now fully specified) and ADR-0044 (D5's own Sonar honesty story extended to suite scale). | The exact CP7 rating-gate floor — resolve once the suite compiles and real scores exist (trigger: the page-object generation gap, below, closes). Whether CP7's security posture ever gates — resolve only via a future, explicit decision. The JaCoCo-coverage-report-submission build task — not built here. The `fetch_measures` Sonar-adapter capability — new build work, does not exist today. CP8's own static-readiness build — new build work, does not exist today. The page-object generation gap that keeps the tracked baseline from compiling (`suite_quality_governance/README.md`'s own recorded finding) — tracked, not built here, and is CP7's own rating-gating prerequisite. |

## 3. Related state changes this baseline produced

- **CP7 (Layer 4, ADR-0047 D1-D6) BUILT — report-only whole-suite Sonar quality governance, no gating, not yet wired (2026-08-06).** `suite_quality_governance/cp7/` (new package): `measures.py::fetch_whole_suite_quality_report(adapter, project_key)` reads the tracked baseline's own already-accumulated Sonar project measures in ONE call (never a per-run re-scan — every promoted asset was already scanned by CP3 at promotion time; Sonar's own project-level measures are a running rollup) and groups them into three named families exactly matching ADR-0047's own literal Decision-text enumerations: generic quality (D3 — `violations`/`bugs`/`code_smells`/`sqale_rating`/`reliability_rating`), security (D4 — `vulnerabilities`/`security_hotspots`/`security_rating`), coverage/duplication (D5 — `coverage`/`duplicated_lines_density`, genuinely unmeasured on the real server today). `Cp7WholeSuiteQualityReport` (`models.py`) carries NO `overall_verdict`/`passed` field anywhere — proven structurally (`__dataclass_fields__`), not merely by never failing — mirroring `Cp5NearDuplicateSweepResult`'s own no-verdict shape one component over. A metric the server has no value for surfaces as `Cp7MeasureFinding(value=None, measured=False)`, never faked as clean (`has_unmeasured_findings` is the one property a consumer needs). **The adapter extension (ADR-0047 D6), not a parallel one:** `automation_engineering.cp3.sonar.adapter.SonarQualityGateAdapter` (CP3's own Protocol) gained a fourth method, `fetch_measures(project_key, metric_keys) -> SonarMeasuresResult`, alongside two new, generic (non-CP7-specific) shapes in `cp3/sonar/models.py` (`SonarMeasure`, `SonarMeasuresResult`) — `StubSonarQualityGateAdapter` gained a scripted `measures=` constructor param mirroring its own existing `result=`/`poll_error=` discipline exactly (raises if unscripted); `LiveSonarQualityGateAdapter.fetch_measures` is a real `GET /api/measures/component` call, fake-tested (`respx`, no live Sonar in `make test`) — confirmed live-observed behavior reproduced exactly: a metric the server's own response array omits becomes `value=None`, never a faked default. CP3's own three existing methods and all 25 of its own pre-existing adapter/gate tests are unmodified and pass unchanged — the Protocol grew a method, nothing about its other three shrank or changed. `make test`: 5491 → 5506 (+15: 3 in `test_automation_engineering_cp3_sonar_stub_adapter.py`, 3 in `test_automation_engineering_cp3_sonar_live_adapter.py`, 9 in the new `test_suite_quality_governance_cp7_measures.py`); `make lint`: clean; mypy (informational): 432 → 432, no rise. **Not wired into any CLI stage or run-state** — this task builds CP7's own component only, the same "components first, a later task wires them" sequencing CP5's own four components went through before the stage-16 wiring task. **Owner:** `suite_quality_governance/cp7/`, `automation_engineering/cp3/sonar/`. **Trigger for the next task:** wire CP7 into the pipeline (report-only, alongside or after CP8); CP8 itself, still unbuilt; the JaCoCo-report-submission prerequisite and the page-object generation gap, both tracked by ADR-0047 as CP7's own rating-gating triggers, neither touched here.
- **CP7/CP8 (Layer 4) FROZEN — ADR-0047 (2026-08-06), graduating the design proposal's own eight open decisions into binding Decision text, the same two-step process ADR-0046 itself completed for CP5.** A companion ADR, not an edit to ADR-0046 (mirrors exactly how ADR-0046 itself is a companion to ADR-0040/ADR-0044/ADR-0045, never an edit to any of them — Decision text is never retroactively edited, only additively amended). CP7: absolute Sonar measures via `/api/measures/component`, never the built-in leak-scoped gate, never an admin-created custom one; report-only across every metric family at first (generic-quality/reliability rating-gating deferred until the suite compiles and real scores exist; security report-only pending an explicit future decision; coverage/duplication report-only pending a JaCoCo-wiring prerequisite); extends the existing `SonarQualityGateAdapter` seam with `fetch_measures`, never a second Protocol. CP8: validates this platform's own real `pom.xml`/`junit-platform.properties`/runner-class config — POM validated against its OWN declarations, never a hardcoded expected-dependency list — and gates immediately, deterministically, unconditional on compile state; distinct from and complementary to CP5-cohesion's own compile check, never merged into one verdict. Additive amendment notes added to ADR-0046 (D1/D8) and ADR-0044 (D5's own Sonar honesty story extended to suite scale). Deferred, tracked, not built here: the CP7 rating-gate floor (trigger: suite compiles + real scores); the JaCoCo-report-submission build task; the `fetch_measures` adapter build; CP8's own static-readiness build; the page-object generation gap that keeps the tracked baseline from compiling today (CP7's own rating-gating prerequisite). `make lint`: clean; `make test`: 5491 passed, unchanged (documentation-only). **Owner:** none yet — a future implementation milestone, per ADR-0047's own authorization.
- **CP7/CP8 (Layer 4, ADR-0046 D8) design proposal committed — the Sonar Community Edition capability discovery D8 flagged as a precondition, done live (2026-08-06).** `docs/proposals/layer-4-cp7-cp8-design.md` — design only, no ADR frozen, no code built. Confirmed directly against the real, locally-running SonarQube server (initially unreachable, then found and reached via `docker ps`/`docker logs` once its container finished booting): edition is genuinely `community` (`/api/navigation/global`'s own `edition` field, not assumed); the server's one built-in quality gate (`Sonar way`) is entirely `new_*`/leak-period-scoped, the wrong tool for a whole-suite check, so CP7's design reads absolute measures via `/api/measures/component` instead (all readable by the pipeline's own least-privilege token); `coverage`/`duplicated_lines_density` are unmeasured today (no JaCoCo XML report ever submitted, though the import plugin is deployed) — flagged as a tracked prerequisite, not gated on; Sonar CE genuinely evaluates 36 security-hotspot + 35 vulnerability Java rules (refining, not overturning, the LLD review's own S3/S6 lean — the review workflow is what a non-admin token can't reach, not the detection itself), so CP7 proposes reporting security metrics rather than hard-gating them. CP8 is designed against this platform's own real `pom.xml`/`junit-platform.properties`/`RunCucumberTest.java`, with an explicit, reasoned boundary against CP5-cohesion's own compile check (CP8 = declared-and-well-formed; CP5-cohesion = actually compiles/resolves) and against Layer 5 (CP8 never executes anything). `make lint`: clean; `make test`: 5491 passed, unchanged (documentation-only). **Owner:** none yet — a future CP7/CP8 design-then-freeze task, per this proposal's own §5 open decisions.
- **`docs/adr/0020-platform-evolution-roadmap.md`** — status line changed from `Proposed` to `Superseded by ADR-0031`. Body untouched (historical record preserved per this batch's constraint against retro-editing Accepted/Proposed ADR bodies).
- **`docs/architecture/architecture-action-register.md`** — ACT-001 changed from `Identified` to `Closed`, Verification Evidence set to ADR-0038, register version bumped to 1.2, metrics updated (Open 8→7, Closed 0→1).
- **`docs/reference/`** — new non-normative documentation tree established (ADR-0040), alongside `docs/integrations/`, `docs/development/`, `docs/operations/` under ADR-0038's operational carve-out. Holds `docs/reference/automation-poc/` — 5 of the sibling POC's ~26–30 Copilot prompt assets and its `.gherkin-lintrc`, reference-only, not loaded by any code.
- **ADR-0042 Decision 4 and ADR-0035 item 2's resolution note** — both extended with removal sequencing: `canonical_requirement.py` is not deleted by the ADR-0035 items 1/3/4 removal task; it is deleted in the same change that creates the `TestableRequirement` contract module, so `SourceRef` moves directly into its new home. "Unblocked" (ADR-0035 item 2) means the salvage question is decided, not that the file may be removed on its own schedule.
- **ADR-0042 Decision 1, additive correction notes (2026-07-25)** — added beneath the `TestableRequirement` table (`priority`), the `AcceptanceCriterion` table (`traces_to`), and the `Risk` table (`category`): `AnalysisResult` — the sole Layer 1 output the contract is populated from — carries no honest per-requirement priority signal, no criterion-to-`SourceArtifact` link, and no per-risk category signal (verified during Part 2 emitter implementation: priority only ever appears formatted *into* the prompt from `SourceArtifact.priority`, never emitted back; the LLM is never shown an artifact identifier and the output schema mandates bare string statements with no citation field, so no AC-level provenance is ever carried, only group-level via `source_consolidated_id`; risks are a flat, uncategorized string array end to end). `priority` and `Risk.category` are corrected to optional/nullable for `contract_version` 1.0.0, emitting `null`; `AcceptanceCriterion.traces_to` emits an empty list, reserved. `TestableRequirement.traces_to` is unaffected and carries the honestly-available group-level provenance. All three are reserved fields whose future population requires a new Layer 1 signal-producing capability, gated behind ADR-0032's freeze-lifting procedure. `contract_version` itself is unchanged (still `1.0.0`): this corrects the spec to match honest Layer 1 capability before any implementation shipped against the defect, not a change to a released contract.
- **ADR-0042 Decision 1, structural correction note (2026-07-25)** — distinct from the three nullability-relaxation notes above: this one changes the contract's **field structure**, not a field's optionality. Layer 1's risks are a single flat, run-level list with no per-requirement attribution — the same non-attribution problem as `AcceptanceCriterion.traces_to`, one level up, where a per-requirement-only home forces a choice between fabricating attribution or discarding honest signal. `risks[]` is added to `TestableRequirementSet` at the set level (matching the data's actual granularity); `TestableRequirement.risks[]` is retained in the schema, reserved, and always empty in `contract_version` 1.0.0. The Part 1 schema-compatibility test's checked-in baseline was regenerated as part of this correction — a pre-release shape fix, made and reported before any consumer shipped against the prior shape, not an unversioned change to a released contract.
- **`docs/adr/0037-generated-test-suite-target-and-sut-binding.md`, two additive resolution notes (2026-07-25)** — added beneath the TBD bullet for the tracked baseline's exact directory location, and beneath the "SUT binding boundary, locked" decision bullets. Note 1: the baseline-path TBD is resolved to `test-suite-baseline/` at repo root, established by the walking-skeleton build committed at `2195fbd`. Note 2: the env-binding boundary is now realized structurally, not just by convention — `config.properties`'s `env.*`/`data.*` namespace split and `ConfigReader`'s separate `env()`/`data()` accessors (`-Denv.<key>` / `TEST_DATA_<KEY>` overrides), deliberately diverging from the POC's flat, mixed-namespace `config.properties`. No Decision line changed; ADR-0037's remaining TBD — the exact promotion review mechanism — is untouched and stays open.
- **`docs/audit/CODEBASE_AUDIT_2026-07-24.md`** — additive correction block appended (Appendix), fixing five verified quantitative errors (commit counts, `requirement_intelligence/` file count, `docs/` markdown count, `tests/unit/` file count) found by a fresh sweep of every numeric claim the audit makes. The commit-count error ("1290" total / "~1260" since initial commit) was the trigger — corrected to 252 (at the audit's own commit `932f416`) / 251 (since `28d0284`) — verified directly, with a history-rewrite alternative explanation explicitly checked and ruled out (no shallow clone, clean reflog, no unreachable commits beyond ordinary stash artifacts). The audit's body is untouched; the correction is additive only. All layer-status, `NotImplementedError`-stub, and dead-scaffolding findings are explicitly unaffected — they were never derived from the corrected numbers. **`docs/adr/0032-layer-1-capability-freeze.md`** — additive verification note added at its own commit-count table, clarifying that ADR-0032 independently re-ran its own count (252, at its own parent commit `4613b40`'s predecessor) rather than citing the audit's figure, so it never carried the audit's error and required no correction — only a note pointing this out. **No ADR decision changed by either edit.**
- **`docs/adr/0043-layer-2-feature-engineering-architecture-freeze.md` (new) — freeze-lift finding.** ADR-0043 investigated whether it could lift ADR-0032's Layer 1 freeze, per the task that produced it. Finding: **it cannot, and does not** — ADR-0032's lifting procedure's precondition 1 ("Layer 2... has reached Runtime Integration") is not met by an architecture-freeze ADR with zero Layer 2 implementation (`feature_engineering/` remains the single-commit placeholder package). ADR-0043 clarifies, separately, that Layer 2's own implementation was never blocked by ADR-0032 in the first place (that freeze restricts only Layer 1's growth) and is authorized by ADR-0043's own Acceptance. Two additive amendment notes recorded this: **`docs/adr/0014-prompt-governance-subsystem.md`** (2026-07-25) — Layer 2 established as a second consumer of the existing prompt registry; the registry's future extraction to a shared location is flagged, not decided, against ADR-0032's five carve-outs. **`docs/adr/0032-layer-1-capability-freeze.md`** (2026-07-25) — records that ADR-0043 is Layer 2's Architecture Freeze (lifecycle stage 1 of 6, not stage 4), that its precondition 1 is therefore unmet, and that the freeze remains in full force until a separate future ADR satisfies all three preconditions. **No Decision line changed in either ADR.**
- **`docs/adr/0043-layer-2-feature-engineering-architecture-freeze.md` D3, additive correction note (2026-07-27).** Found during the Layer 2 Gherkin-linter build (the first Layer 2 implementation against this ADR): D3's "5 rules operate on raw source text" claim is corrected to **3** (`no-trailing-spaces`, `no-multiple-empty-lines`, `new-line-at-eof`), verified against upstream `gherkin-lint`'s own source and empirically against the committed port. `indentation` and `no-partially-commented-tag-lines` — named by D3 as raw-source rules — in fact operate purely on the parsed AST. D3's decision and its architectural requirement ("do not build an AST-only linter") are unaffected: the port gives all 17 rules uniform access to both raw lines and the AST regardless of which each rule uses. **No Decision line changed.**
- **`docs/adr/0043-layer-2-feature-engineering-architecture-freeze.md` D2, additive clarification note (2026-07-27).** Found during the Layer 2 feature-generator build (the first Layer 2 implementation to consume D2's prompt-conversion guidance): D2's wording conflated two distinct properties of platform-assigned ids — "platform-assigned, content-addressed, never-LLM-assigned" (true of all four: `REQ-*`/`AC-*`/`RSK-*`/`SCN-*`) versus "assigned after the remediation loop" (true of `SCN-*` only). `REQ-*`/`AC-*` are minted by Layer 1 (`contracts/testable_requirement.py`'s `build_testable_requirement`) and are already known at Layer 2 generation time; the conflation had already produced one wrong implementation — `generate_feature` v1.0.0's `@REQ-PENDING`/`@AC-PENDING` placeholders for values that were never actually deferred, corrected in v1.1.0. The note also records a second, independent reason `@REQ-*` is never model-emitted: a tag immediately before a `Background:` block is a hard Gherkin parse error. **No Decision line changed.**
- **`docs/adr/0043-layer-2-feature-engineering-architecture-freeze.md` D2, additive hoist-extension note (2026-07-27).** The first live-model run against the generation core (`REQ-6e894aae`) surfaced a `no-homogenous-tags` lint failure the original hoist rule did not cover: functional tags (`@inventory`, `@smoke`) on a single-scenario feature are trivially homogenous but were never hoisted, since D2's rule only ever considered id tags (`@AC-*`/`@SCN-*`). Extended, additively: **any** tag homogenous across every scenario in a feature — id or functional — now hoists to Feature level; Cucumber resolves Feature-level and scenario-level tags identically for tag-filtered selection, so this changes no test-execution behavior, and `.gherkin-lintrc` stays byte-unchanged (resolved generation-side, honoring D3's no-rule-changes lock). Named sharp edge: a functional tag on every scenario of a feature (trivially true for any single-scenario feature) now lives at Feature level, not scenario level — a future consumer expecting scenario-scoped functional tags must check feature-scope tags too. D2's id-assignment decision is unchanged; only the hoist mechanism's scope widened. Implemented in `feature_engineering/generation/assembler.py` (`per_scenario_all_tags`, generalized from `per_scenario_id_tags`); covered by `tests/unit/test_feature_engineering_hoist_and_feature_name.py`.
- **Feature-name derivation added: `feature_engineering/generation/assembler.py`'s `derive_feature_name` (2026-07-27, ADR-0043 D1, no ADR text changed).** The same live run's `name-length` failure (`REQ-6e894aae`'s 121-char title against `.gherkin-lintrc`'s 70-char Feature limit) exercised, for the first time, the title-overflow handling `contracts/testable_requirement.py`'s own `title` field docstring already anticipated: Layer 1 emits the full, unconstrained title; Layer 2 derives its own lint-compliant Feature name from it. `derive_feature_name` is a deterministic, stable transform (same `(title, requirement_id)` always yields the same result); when `title` already fits, it passes through unchanged with no side effect (existing short-title fixtures are byte-identical). When it does not fit, the name is truncated and the requirement's own already-unique `requirement_id` is appended as a disambiguating suffix (two distinct over-length titles sharing a truncation prefix cannot collide, since `requirement_id` is content-addressed over each one's *full* title); the full title is never discarded, preserved verbatim as a leading Gherkin comment. `requirement_id`/`content_hash` are unaffected — both are already fixed by the time this transform runs, and only `title` (never the derived name) feeds `REQ-*` (ADR-0042 Decision 2). Not a fix to Layer 1's `title` field and no contract change. Covered by `tests/unit/test_feature_engineering_hoist_and_feature_name.py`.
- **Prompt registry framework relocated: `requirement_intelligence/prompts/{framework,models}` → `shared/prompts/{framework,models}` (2026-07-27, ADR-0043 D4 carve-out-3).** ADR-0043 D4's forcing clause fired during Layer 2's prompt-conversion work (registering a Layer 2 prompt required importing the registry) and was resolved as a no-behavior-change relocation, not a freeze-lift precondition — the mechanism (`PromptRegistry`, `PromptLoader`, exceptions, the governed template contract, and the five canonical models) is generic infrastructure with no Layer-1 judgement logic. Layer 1's own content (`composition.py`'s hardcoded registrations, `versions/`, constants, templates, the builder) stayed at `requirement_intelligence/prompts/`, now itself a consumer of the shared mechanism. Verified byte-identical: 4820 tests before and after (unchanged), golden-baseline `test_determinism_prompt_sha256` passing, `PromptLoader` SHA-256 verification against the untouched `versions/manifest.json` still succeeding. Two Layer-1-shaped elements moved **unchanged**, not generalized — see §4 item 12. Additive notes: `docs/adr/0014-prompt-governance-subsystem.md`, `docs/adr/0043-...md` D4, `docs/governance/platform-capability-matrix.md` (CAP-072/073), `docs/governance/architecture-freeze-index.md`. **No Decision line changed in any ADR.**
- **`docs/adr/0043-layer-2-feature-engineering-architecture-freeze.md` D2/D5, tag-assignment-ordering resolution (additive, 2026-07-27).** A three-way tension was found and verified directly: D2's decision and D5's pipeline-order prose both place `SCN-*`/tag assignment *after* the remediation loop, but the already-built generator (`assembler.py`'s `generate_feature_file`) mints and assembles all tags in one pass *before* any remediation loop runs, and the already-registered `fix_gherkin_lint` v1.0.0 prompt's own contract requires already-tagged input ("Preserve every @REQ-*, @SCN-*, and @AC-* tag exactly as given"). Resolved: **assign-during-generation is authoritative** — remediation preserves existing ids rather than tagging afterward; this is judged the better design, since it lets the remediator see and honor the traceability structure it must not break. D5's pipeline-order prose is corrected accordingly (short cross-reference note at D5, full resolution at D2); neither D2's nor D5's *decision* changes. D2's original orphaned-id concern is confirmed still honored — via **re-derivation** (every remediation attempt is independently re-parsed and re-CP2-gated from its own text, `feature_engineering/remediation/loop.py`'s `_rebuild_generated_feature`; a failed attempt's ids never surface), not the deferred-assignment mechanism D5's prose described. One sub-case is specified but not implemented: a remediation-created (split) scenario's `SCN-*` must be platform-minted fresh via the existing content-addressed mechanism, never LLM-chosen or copied — flagged as a future-implementation item tied to the live-remediator follow-up (§4 item 13 below). **No Decision line changed in either D2 or D5.**
- **Layer 2 deferred-items decision executed (2026-07-29): two items closed, one formally deferred.** §4 items 12 and 13 updated in place (see above); no ADR Decision line changed by any of it — item 13 executes what ADR-0043 D2's 2026-07-27 addendum already specified, item 12 executes and defers what the prompt-registry relocation already flagged. **Item 13** (split-scenario `SCN-*` re-mint) is now **IMPLEMENTED** — `feature_engineering/remediation/loop.py`'s `_remint_split_scenario_ids`, wired into the Tier-2 remediation loop, tag-based identity, reusing `contracts.id_generation.generate_scenario_ids` (no second id-minting path introduced) — construct-tested only (`tests/unit/test_feature_engineering_remediation.py::TestSplitScenarioRemint`, 3 tests); two live runs (60 generations) have produced zero real splits, so this stays *implemented, never observed live*, not *proven*. **Item 12(a)** (the placeholder) is **DONE** — `ARTIFACT_CONTEXT_PLACEHOLDER` renamed to `CONTEXT_PLACEHOLDER` (symbol only; its `"{artifact_context}"` value, an external contract every committed template embeds literally, is unchanged), every reference updated. **Item 12(b)** (`PromptCompatibility`) is **DEFERRED to Layer 3** — its trigger is recorded at item 12 above. `make test`: 4980 → 4983 (all +3 from item 13's new tests; the placeholder rename adds none). `make lint`: clean. mypy (informational): 432 → 432, no rise. **Layer 2's two remaining open items, each with a stated trigger:** the promotion mechanism (workspace → tracked baseline, ADR-0037's own remaining TBD — §4 item 6) is Layer 4's to define, untouched by this round; `PromptCompatibility`'s generalization (item 12(b)) is Layer 3's, per the trigger recorded there. Separately noted, not corrected here: ADR-0043's own "Recommendations (permanent)" item 2 ("No `SCN-*` id is ever assigned before the remediation loop completes") still carries D5's original, since-corrected pipeline-order prose — D2's own later additive note (2026-07-27) resolves assign-during-generation as authoritative and this task's implementation follows that later, more specific, code-verified resolution, not Recommendation 2's stale wording; the ADR's own internal inconsistency is flagged for a future correction, not resolved by this entry.
- **`docs/adr/0042-testablerequirement-field-specification.md`, `TestableRequirement.traces_to` granularity sharpened (additive, 2026-07-28).** A Layer 1 input-fixture rebuild task (rebuilding the JIRA/SonarQube/ZAP FILE-mode fixtures around saucedemo.com) verified directly against `requirement_intelligence/testable_requirement/emitter.py::_source_refs`, and empirically against a live end-to-end run (30 requirements, 25 deduplicated `SourceRef`s), that `TestableRequirement.traces_to` is **run-scoped, not group-scoped**: every requirement in a `TestableRequirementSet` carries the identical, full, deduplicated `SourceRef` set the run's `EngineeringContext` evidence assembled across *every* contributing consolidation group — not just the group that produced that requirement's own statement. ADR-0042's existing "requirement→group" wording (Decision 1, `AcceptanceCriterion.traces_to` correction note) undersold the coarseness; the honest granularity is requirement→run. No field shape or Decision changed — `SourceRef[]` is unaffected — only the granularity description is corrected. Consequence recorded for future consumers: no per-requirement source attribution exists at any finer granularity than the run, so a future Layer 7 (or any `traces_to` consumer) must not be designed to expect it; and in a run mixing real and fixture-provenance sources, every requirement's `traces_to` carries the fixtures too once any fixture artifact reaches the run's evidence, so fixture-vs-real provenance must be judged by requirement content, never by filtering `traces_to`. See the additive note in ADR-0042 itself for the full statement.
- **Layer 3 (Automation Engineering) LLD committed to `docs/proposals/` with a Reviewer's note (2026-07-29).** `docs/proposals/layer-3-automation-engineering-lld.md` — a faithful, unedited transcription of the 26-slide `Automation_Engineering_Layer.pptx` deck (committed alongside as provenance, same pattern as `docs/proposals/layer-2-feature-engineering-lld.md`), governing nothing yet, documentation-only. Its own Reviewer's note records three superseded/corrected items (S1: the deck's "Test Generator" component, which emits `@RunWith(Cucumber.class)` runner classes, is superseded by ADR-0041 D3's prohibition of `@RunWith`/`@CucumberOptions` and deleted as a code generator — the tracked `RunCucumberTest.java` already executes every feature; S2: the deck's slide-23 task breakdown is Layer 2's own task list pasted wholesale, not a Layer 3 guide; S7: the deck's CP5/CP6 references are Layer 4/Layer 5 scope, out of Layer 3's own freeze). Four architecture questions plus the reuse-safety model are **resolved pre-freeze**, recorded as locked decisions pending the still-future Layer 3 architecture-freeze ADR: **Q1** deletes the Test Generator; **Q2** locks the asset catalog to a persistent, baseline-reconciled store (Model B) with semantic (not exact-string) match, trusted only when match confidence clears a threshold (else escalate to human review), the catalogued code's content-hash still validates, and the step definition's signature fits the Gherkin step it binds to — all three required because a wrong semantic binding is the one Layer 3 failure mode that runs green while testing the wrong behavior, invisible to every downstream deterministic gate; **Q3** scopes CP4 to static-only locator health from page-object source, no running-browser/SUT dependency, live-DOM validation left to Layer 5; **Q4** makes CP3 gate on step/scenario coverage and duplicate/unmapped-step counts only, with reuse % reported but never gated (mirrors ADR-0043 D5's exact-floor replacement of the Layer 2 LLD's soft ">95%" thresholds). Remaining TBDs (test-data spec→Java mapping, SonarQube/CP3 alignment, semantic-match embeddings-vs-LLM choice, catalog storage location) are deferred to the freeze ADR or implementation, not resolved here. **No ADR Decision line changed — this is a new proposal document, not an amendment to any existing ADR.**

- **ADR-0044 (Layer 3 Automation Engineering Architecture Freeze) Accepted (2026-07-29).** Turns the reviewed Layer 3 LLD (§3 entry above) into locked design: no Test Generator (ADR-0041 D3's `@RunWith`/`@CucumberOptions` prohibition); a persistent, semantic asset catalog (Model B) reconciled from the tracked baseline at run start; the reuse-safety model (a semantic binding trusted only when confidence, content-hash, and signature-fit all hold, else escalate — the one Layer 3 failure mode invisible to every downstream deterministic gate); CP3 gating on step/scenario coverage and duplicate/unmapped-step counts, reuse % reported not gated, and a hard SonarQube quality-gate requirement against the `customqa:*` profile; CP4 gating on static locator health only, no SUT dependency; Java test-data classes generated from Layer 2's specification (ADR-0043 D7), never re-derived; and Layer 3's placement at ADR-0036 stage 15. Two additive amendment notes recorded this: **`docs/adr/0040-control-point-model-and-layer-4-redefinition.md`** (2026-07-29) — specifies Decision 1's CP3/SonarQube assignment as a hard gate, records its dual role as a generation constraint, and records the stub-vs-live test story; also reconciles an apparent tension with ADR-0037's own "Layer 4 asset" phrasing for the `customqa:*` profile (stewardship, unchanged) against ADR-0040 Decision 1's assignment of its *evaluation* to CP3/Layer 3 (which this note confirms and specifies) — read as answering different questions, not as a contradiction requiring either ADR's Decision text to change. **`docs/adr/0043-layer-2-feature-engineering-architecture-freeze.md`** (2026-07-29) — records that the `PromptCompatibility` generalization trigger (§4 item 12(b) below) has fired; the generalization itself remains deferred to Layer 3's implementation. **No Decision line changed in either amended ADR.**

- **ADR-0045 (Scoped Asset Promotion — Layer 3 Capstone, pulled from Layer 4) Accepted (2026-07-30).** Resolves ADR-0037's long-open promotion TBD, pulled forward from a future Layer 4 build into Layer 3's own capstone build step because Layer 3's persistent reuse catalog (ADR-0044 D3) cannot accumulate reuse across runs without a promotion mechanism existing. Locks: the promotable gate (CP2 pass on the owning feature, CP3+CP4 pass on the automation, and confirmed non-duplicate status against the tracked baseline, using ADR-0044 D3/D4's own content-hash/signature identity computation — no new duplicate detector); the review model (clean, validated, non-duplicate assets auto-promote; ADR-0044 D4 escalations — low confidence, stale content-hash, signature mismatch — wait for that same human-in-the-loop review, one queue not two); the build-order placement (catalog → reuse engine → generators → CP3 → CP4 → promotion, promotion last); and the mechanism at the decision level only (a git-visible copy from workspace to tracked baseline; auto-commit vs. stage-for-review left TBD-at-implementation). Explicitly scopes out Layer 4's broader suite-level governance — orphaned-glue detection, the suite-wide near-duplicate sweep, and the aggregate release gate all remain ADR-0040 Decision 3's, unclaimed by this ADR; Layer 4 may later wrap this promotion decision in richer governance, but is not required to. Two additive amendment notes recorded this: **`docs/adr/0037-generated-test-suite-target-and-sut-binding.md`** (2026-07-30) — three notes added: beneath the "Two-tier split, locked" promotion bullet (narrowing "never automatic," not reversing it), beneath the promotion-TBD bullet itself (marking it resolved), and beneath Recommendation 4 (recording that ADR-0045 is the future ADR Recommendation 4 itself anticipated as the only sanctioned way to revisit "never automated"). **`docs/adr/0040-control-point-model-and-layer-4-redefinition.md`** (2026-07-30) — a note added after Decision 3's list, recording that only the promotion decision is pulled forward into Layer 3, not Layer 4's broader suite-level integration governance remit, which this Decision's list otherwise still fully governs. ADR-0044's own "does not own... the promotion mechanism" language (its Consequences/Ownership sections) is reconciled by ADR-0045's own text, not by editing ADR-0044's body — the same non-retroactive-editing, reconcile-in-the-newer-ADR pattern ADR-0044 D5 already used for the ADR-0037/ADR-0040 "Layer 4 asset" tension. **No Decision line changed in either amended ADR.**

- **Layer 3 page-object generator built; the precise method-fit obligation DISCHARGED for page objects (2026-07-31).** Two builds landed together. (1) A `PageObjectGenerator` seam (`automation_engineering/generation/page_object_generator.py`, `live_page_object_generator.py`) plus a reuse-first orchestrator (`page_object_orchestrator.py`) generating into `com.automation.pages`, extending `BasePage`, constructor-injected `WebDriver` per ADR-0041 D5 — the same NO_MATCH-generates/TRUSTED_REUSE-binds/ESCALATION-surfaces shape the step-def generator already established, with the SAME two evidenced `customqa:*` rules injected (`customqa:direct-webdriver-action`, `customqa:long-method` — here constraining HOW WebDriver is used, since page objects are where it legitimately lives, the opposite framing from step definitions). A second `generate_page_objects` v1.0.0 prompt registered against the shared registry, declaring the same two Layer-3-own `PromptCompatibility` dimensions `generate_step_definitions` already introduced. (2) **The precise method-fit obligation ADR-0044 D4's clarification note recorded — "a tracked obligation on the future generator task, not yet built" — is now discharged, for page objects.** `automation_engineering/generation/method_fit.py`'s `verify_specific_method_fit` checks a caller-named specific method against a `TrustedReuse.asset.methods` inventory (present + shape-fitting, or `Escalation(METHOD_FIT)`), wired into the step-def orchestrator's own NO_MATCH branch via an optional `page_object_request` parameter — the ACTUAL binding point the note named. Proven directly against the note's own illustrative example (a `LoginPage` with only `open()`, reused for a step needing `clickForgotPasswordLink()`): the reuse engine itself returns `TrustedReuse` (coarse genuinely passes), the precise check still escalates, the step-def seam is never called. Check (c) is now fully realized for page objects — coarse (decision-time) **and** precise (generation-time). Utilities remain the next build's own, still-open obligation, honestly carried forward the same way this one was (`verify_specific_method_fit` is written against the same `PageObjectAsset | UtilityAsset` union the coarse screen already uses, but no orchestration built here ever calls it with a `UtilityAsset`). `automation_engineering/reuse/engine.py`'s private `_method_shape_fits` was promoted to public `method_shape_fits` (behavior unchanged) so both the coarse screen and this new precise check share one shape-compatibility rule rather than two hand-maintained copies. An additive discharge note is added directly beneath ADR-0044 D4's own clarification note (see that ADR). `make test`: 5109 → 5155 (+46: 40 new across the page-object generator/orchestrator test modules, 2 new step-def orchestrator wiring proofs, 4 new prompt-registration proofs); `make lint`: clean; mypy (informational): 432 → 432, no rise.

- **Layer 3 utilities generator built — the last of the three Gherkin-derived generators; the precise method-fit obligation DISCHARGED for utilities too, investigated not assumed (2026-07-31).** Before building, this task examined (rather than inherited) whether utilities carry the SAME precise-method-fit risk page objects do — the prior note's own "utilities remain... still-open" framing repeated ADR-0044 D4's own undifferentiated "page objects / utilities" text without independently establishing the risk is real for utilities specifically. **Finding: it is, and sharper than for page objects.** The one real utility class this platform has ever committed, `test-suite-baseline/.../base/ConfigReader.java`, exposes `env(String)` and `data(String)` — semantically distinct (SUT-binding vs. test-data, ADR-0037 D3) yet IDENTICALLY shaped (`String -> String`), a pair a coarse arity/type screen structurally cannot tell apart. A step needing `ConfigReader.data("username")` reused against a candidate whose only relevantly-shaped method is `env` clears the coarse screen and binds to the wrong accessor. Built: (1) a `UtilityGenerator` seam (`automation_engineering/generation/utility_generator.py`, `live_utility_generator.py`) plus a reuse-first orchestrator (`utility_orchestrator.py`) generating into `com.automation.utils`, matching the real `ConfigReader` shape (final class, private constructor, static methods only) — same NO_MATCH-generates/TRUSTED_REUSE-binds/ESCALATION-surfaces shape as the other two generators. Only `customqa:long-method` is injected as an evidenced customqa:* constraint — `customqa:direct-webdriver-action`'s own evidenced target and message are page-object-specific, so the WebDriver prohibition for utilities is stated as a plain architectural fact, not dressed up as a customqa:* rule never evidenced against a non-page-object file. A third `generate_utilities` v1.0.0 prompt registered, declaring the same two Layer-3-own `PromptCompatibility` dimensions the other two prompts already use. (2) `automation_engineering/generation/method_fit.py`'s `verify_specific_method_fit` is REUSED UNCHANGED — it was already written generically over `PageObjectAsset | UtilityAsset` for exactly this reason — wired into `orchestrator.py`'s own NO_MATCH branch via a second optional parameter, `utility_request`, alongside `page_object_request` (either escalating diverts the whole step). Proven directly against the `env`/`data` scenario: the reuse engine itself returns `TrustedReuse` (coarse genuinely passes), the precise check still escalates, the utility seam is never called. **All three catalog asset kinds (D3) are now legitimately handled — there is no fourth, still-deferred kind.** The prior structural AST guard ("this module never references `catalog.page_objects`/`catalog.utilities`") is retired and replaced with a positive completeness check. An additive discharge note is added directly beneath ADR-0044 D4's own clarification note (see that ADR, immediately following the page-object discharge note). `make test`: 5155 → 5201 (+46: 38 new across the utility generator/orchestrator test modules, 2 new step-def orchestrator wiring proofs, 6 new prompt-registration proofs); `make lint`: clean; mypy (informational): 432 → 432, no rise (`git stash -u` baseline re-verified directly).

- **Layer 3 test-data generator built — the fourth and last generator; it deliberately BREAKS the triad pattern, and two findings surfaced along the way (2026-07-31).** Its input is Layer 2's own test-data SPECIFICATION (ADR-0044 D7 / ADR-0043 D7 — "which fields are needed, and for each, whether positive/negative/boundary variants are required"), never a Gherkin step need. **Finding 1 — "already-registered" was imprecise.** Both D7 ADRs call `generate-test-data` "the already-registered" prompt; true only of the ORIGINAL POC's own catalog (`docs/reference/automation-poc/prompts/generate-test-data.md`) — zero registrations for it ever existed in this platform's actual governed registry before this build. This build performs that registration (`generate_test_data` v1.0.0), reconciling the POC's own stale `ConfigReader.get(...)` call (the tracked baseline's real `ConfigReader` never had that method) against the walking skeleton's actual `env()`/`data()` split. **Finding 2 — Layer 2's own D7 specification emission is a locked DECISION, never an IMPLEMENTED one.** A repo-wide search for `TestDataSpec`/`test_data_spec`-shaped code in `feature_engineering/` returns nothing. `TestDataSpecification`/`TestDataFieldSpec` (`automation_engineering/generation/models.py`, `required_variants` reusing `PolarityHint` unchanged) are therefore modeled on the CONSUMING side, matching D7's own prose contract field-for-field; this generator is built and proven against that contract, not a real Layer 2 emission — the real end-to-end path remains not exercisable, a Layer 2 gap out of this build's own scope. **The reuse question, resolved by investigation:** test-data does NOT get ADR-0044 D3's reuse-first treatment — `AssetKind` names exactly three reusable-asset kinds (none of them test data), and D1's own listing names "Java test-data classes" as a deliverable SEPARATE from that trio. `automation_engineering/generation/test_data_orchestrator.py` never imports the reuse engine; every specification generates, every time (proven for multiple independent specifications, never a skip). **The env/data (ADR-0037 D3) boundary is enforced, not merely requested:** a deterministic, orchestration-level guard raises `TestDataBoundaryError` on any `ConfigReader.env(...)` call or `env.`-prefixed config-key literal in generated output, on top of the prompt's own CONSTRAINTS section — proven for both a violating and a compliant case. Only `customqa:long-method` is injected (`customqa:direct-webdriver-action` not fabricated — test data never touches WebDriver, the same honest-negative discipline the utilities build established). An additive note is added directly beneath ADR-0044 D7 (see that ADR). `make test`: 5201 → 5238 (+37: 30 new across the test-data generator/orchestrator test modules, 7 new prompt-registration proofs); `make lint`: clean; mypy (informational): 432 → 432, no rise (`git stash -u` baseline re-verified directly).

- **Layer 3 promotion built — the capstone, the LAST Layer 3 build step; the reuse loop closes end-to-end (2026-08-02).** `automation_engineering/promotion/` implements ADR-0045 D1–D5 in full, scoped exactly to promotion (D1: no orphaned-glue detection, no suite-wide near-duplicate sweep, no aggregate release gate — those stay Layer 4's). **Identity (D2(b)):** `identity.py::resolve_candidate_identity` derives a candidate's content-hash/class-name/destination path by writing its `java_source` into a throwaway single-file baseline layout and running the REAL `automation_engineering.catalog.scanner.reconcile` against it — never a hand-rolled parse, so a step-definition's per-method hash and a page-object's/utility's per-class hash are computed by the exact same rules the tracked baseline itself reconciles against. **The gate (D2):** `gate.py::evaluate_promotion` checks (a) CP2 (per-feature)/CP3/CP4 (per-run-batch, the granularity those gates are actually computed at today — see the function's own docstring for why) all pass, THEN (b) `AssetCatalog.by_content_hash` (that method's own docstring: "the identity lookup ADR-0045 D2(b) reuses") finds no existing baseline entry under the candidate's exact content-hash — reusing, never re-implementing, ADR-0044 D3/D4's own identity mechanism. **The review model (D3):** `outcomes.py::promote_outcome` dispatches generation's own outcome unions — a `Bound*` outcome (already in the baseline) is never a candidate at all (`None`); an `Escalated*` outcome (any of the three asset kinds) is carried through UNEDITED as `PromotionEscalated`, proven (by object identity, `is`) to be the SAME `Escalation` record the reuse engine produced, joining that one queue, never a second. **The mechanism (D5), auto-commit-vs-stage TBD RESOLVED to stage-for-review:** `mechanism.py::apply_promotion` writes the promoted candidate's exact `java_source` into the tracked baseline at its resolved path; `stage_promoted_assets` `git add`s it and never calls `git commit` — rationale: D3 (is this asset safe to promote) and D5 (does the resulting git history need a human's eyes before it commits) are different questions, and ADR-0037's original "explicit, reviewed step" language was about the latter; proven not to cost the reuse loop anything, since `reconcile()` reads the working tree, not git's index or commit history. **THE LOOP-CLOSING PROOF** (`tests/unit/test_automation_engineering_promotion_loop.py`, one test, real code throughout except the semantic matcher's own stub seam): run 1 reconciles an empty baseline (0% reuse, correctly), generates a step definition, gates it clean, finds no duplicate, PROMOTES it, stages the git-visible change (verified via `git status --porcelain`/`git diff --cached`, and that `git log` gained no new commit); run 2 reconciles the SAME now-non-empty baseline, finds the promoted asset (proving staging alone — no commit needed — already closes the loop), the reuse engine now returns `TrustedReuse` for the identical step-need, and the step-def orchestrator BINDS rather than regenerates (a spy generator fails the test if ever called) — reuse for that need goes from 0% (run 1) to 100% (run 2). Also proven, independently: each of D2's gate conditions (CP2/CP3/CP4 failure, a content-hash duplicate) blocks promotion on its own, a gate failure is reported ahead of a duplicate when a candidate is both, and all three generated-asset kinds (step definition, page object, utility) resolve and gate identically through the same dispatcher. `make test`: 5292 → 5324 (+32: 8 identity, 8 gate, 11 outcomes-dispatch, 4 mechanism, 1 loop-closing proof); `make lint`: clean; mypy (informational): 432 → 432, no rise; new code (`automation_engineering/promotion/`) itself fully clean under mypy.

- **Layer 4 (Quality Governance) LLD committed to `docs/proposals/` with a Reviewer's note (2026-08-06) — review is underway, no ADR frozen.** `docs/proposals/layer-4-quality-governance-lld.md` — a transcription of the reviewed 21-slide `Quality_Governance_Layer.pptx` deck (not committed to this repository, unlike the Layer 2/3 decks; built from the completed slide-by-slide review rather than a direct re-read of the file), governing nothing yet, documentation-only. **Headline finding (Finding 0):** the deck's own "CP5" (whole-suite Sonar governance) and "CP6" (execution-readiness) labels conflict with two Accepted ADRs — ADR-0040 Decision 3 and ADR-0044 both already assign CP5 to Layer 4's suite-level integration governance (orphaned-glue detection, cross-suite near-duplicate sweep, suite compilation, aggregate release gate) and CP6 to Layer 5's own execution control point, undivided. Per this review's own governing instruction, the ADRs win: the deck's content is real (S1's suite-vs-per-run Sonar distinction and S2's static/dynamic readiness split both confirmed as sound boundaries, mirroring CP3/CP4's own per-run and static/live splits) but its CP5/CP6 labels cannot stand as given — a future freeze must rename them. A second, unrelated name collision is also flagged (Finding 0a): `docs/proposals/quality-governance-framework.md` (ADR-0017, CAP-080) is an already-live, already-wired subsystem also named "Quality Governance," scoped entirely differently (post-CP1, Layer 1) — no functional overlap, but a standing source of reader confusion. **The true two-half scope (Finding 1):** half (a) is this deck's own content (whole-suite Sonar + execution readiness, S1–S6 boundary fixes recorded — S3 scopes CP5's security dimension down to inapplicable-to-test-code honesty mirroring CP3's `customqa:*` story, S4 drops the deck's Governance Dashboard slide to Layer 7, S6 flags Sonar Community Edition's claimed capabilities as to-verify-before-building); half (b) is suite-integration governance (orphaned-glue detection, cross-suite near-duplicate sweep, promotion-wrapping, aggregate-release cohesion) — entirely missing from the deck, and per Finding 0 this is not a second gap alongside (a) but the Accepted CP5 itself. ADR-0045 D1's own text is the evidentiary basis that half (b) is reserved, not invented: "This ADR resolves only promotion... It does not pull forward any of Layer 4's other suite-level integration governance (ADR-0040 Decision 3): orphaned-glue detection... cross-suite near-duplicate analysis... the aggregate release gate... All three remain Layer 4's... This is designed to be wrapped, not replaced." Half (b)'s connections to already-built machinery are recorded (Finding 2): orphaned-glue detection as the inverse of Layer 3's catalog/reuse-need matching; the cross-suite near-duplicate sweep as the whole-suite version of ADR-0045 D2(b)'s per-asset check, reusing the same reuse-engine identity mechanism (ADR-0044 D3/D4) at suite scale; promotion-wrapping as a suite-level gate on top of the per-asset promotion mechanism already closed end-to-end (`automation_engineering/promotion/`, 2026-08-02). **No ADR Decision line changed — this is a new proposal document plus a review, not an amendment to any existing ADR, and does not freeze anything.** `make lint`: clean; `make test`: 5403 passed, unchanged (documentation-only).

- **CP5 (Layer 4 suite-integration governance) design proposal committed (2026-08-06) — design only, no ADR frozen, no code built.** `docs/proposals/layer-4-cp5-suite-integration-governance-design.md` designs the four items ADR-0040 Decision 3 names as CP5's own remit — orphaned-glue detection, cross-suite near-duplicate sweep, promotion-wrapping, aggregate-release cohesion — reporting first what CP5 must wrap (ADR-0045's `evaluate_promotion`/`apply_promotion`/`stage_promoted_assets` interface; `scanner.reconcile`'s working-tree-not-git-history read; `LiveSemanticMatcher`'s step-definition-only scope and its real calibration bands — unrelated `[0.52,0.67]`, paraphrase `0.8980`, exact `0.9843`; `stage/gherkin_needs.py`'s per-run-scoped derivation). **Orphaned-glue** is designed to determine "referenced" via deterministic Cucumber pattern-vs-literal-text matching (new: a pattern evaluator, not yet built) rather than semantic similarity, specifically so it can genuinely gate under ADR-0040 Decision 2's deterministic-only rule; semantic similarity is demoted to an advisory secondary hint. **The near-dup sweep** is scoped to step definitions only (matching ADR-0040 Decision 3's own literal text and `LiveSemanticMatcher`'s existing scope, a reported alignment not a coincidence), reuses the embedding boundary's one-batched-call cost shape (O(1) API calls, O(n²) in-memory cosine compute only — a non-issue at this repo's real current scale of 34 step-def-bearing files), and leans a `~0.90` cluster threshold anchored to the real `0.8980` paraphrase measurement already on record. **Promotion-wrapping** is designed to run AFTER per-asset promotion stages (`git add`, no commit) and BEFORE any commit, exploiting `reconcile()`'s own working-tree read to see the assembled post-staging state with no new plumbing — a suite-level reject leaves the staged diff untouched, routed to the same shared human-review queue ADR-0045 D3 already established, never a second queue. **Aggregate cohesion** draws the compile-vs-run boundary at the same live-tool-but-not-live-SUT line CP3/CP4 already drew (CP3's live Sonar server precedent) — "does it compile" leans Layer 4, "does it run" stays Layer 5 — and proposes exact-pattern-collision-across-classes as an achievable first version of ambiguous-glue detection, with full Cucumber Expression overlap analysis flagged as an unscoped stretch goal. Eight open decisions are surfaced explicitly for a future freeze to confirm or change (§4 of the document), none locked here. **No ADR Decision line changed — this is a new design proposal, not an amendment, and authorizes no build.** `make lint`: clean; `make test`: 5403 passed, unchanged (documentation-only).

- **ADR-0046 (Layer 4 Quality Governance Architecture Freeze) Accepted (2026-08-06) — the CP5 design proposal is now frozen; documentation-only, no code built.** Turns the two documents above into locked design. **CP5 is fully specified (D1–D7):** control-point numbering resolves the LLD's own Finding 0 — CP5 stays Layer 4's suite-integration governance (ADR-0040 Decision 3/ADR-0044, unchanged), CP6 stays Layer 5's execution control point (unchanged), and the deck's mislabeled "CP5" (whole-suite Sonar) and "CP6" (execution-readiness) are renumbered CP7 and CP8 — new, non-colliding numbers, named and scoped only, not designed in detail here (D8). All eight of the design proposal's open decisions are locked: orphaned-glue detection gates on deterministic Cucumber pattern-vs-literal-text matching, with semantic similarity demoted to an advisory-only secondary hint, referenced-by-set is the whole current tracked feature corpus, action is flag-for-review-never-auto-remove (D2); the cross-suite near-dup sweep stays step-definition-only, reuses the embedding boundary's one-batched-call shape (`_cosine_similarity`/`_embedding_text` promoted to shared code, mirroring the `method_shape_fits` precedent), defaults to a configurable 0.90 threshold anchored to the real 0.8980 paraphrase measurement, and never auto-merges (D3); promotion-wrapping runs after ADR-0045's per-asset promotion stages (`git add`) and before commit, exploiting `reconcile()`'s working-tree read, and routes a suite-level reject into ADR-0045 D3's own shared review queue, never a second one (D4); aggregate cohesion puts "compiles" on Layer 4 (mirroring CP3's live-tool-not-live-SUT precedent) and "runs" on Layer 5, with ambiguous-glue v1 as deterministic exact-pattern collision across classes (D5). CP5's own PASS/FAIL composition is locked explicit: deterministic components gate, the embeddings-backed near-dup sweep and any semantic orphan hint are advisory-only, exactly mirroring ADR-0040 Decision 2's CP2 precedent (D6). CP5 reuses the catalog, the embedding boundary, the Gherkin-needs derivation, and ADR-0045's promotion mechanism — no second scanner, matcher, or review queue is ever built for it (D7). Governance reporting stays Layer 4's; the dashboard stays Layer 7's (D9, restating the LLD review's S4). The unrelated CAP-080 "Quality Governance" (ADR-0017) name collision is disambiguated, not merged or renamed (D10). Three additive amendment notes recorded this: **`docs/adr/0040-control-point-model-and-layer-4-redefinition.md`** (near Decision 3 — the suite-level integration governance list is now specified in full as CP5; the deck's mislabeled content is renumbered CP7/CP8, not a CP5/CP6 reassignment). **`docs/adr/0044-layer-3-automation-engineering-architecture-freeze.md`** (near its own "does not own... CP5 and CP6" statement — confirmed and specified in full by ADR-0046; its own D1–D8 unchanged). **`docs/adr/0045-scoped-asset-promotion.md`** (near D1's own "designed to be wrapped, not replaced" — the anticipated wrapping is now designed and locked; D1–D5 unchanged, not re-litigated). **No existing ADR's own Decision text changed by this ADR or by any of its three amendment notes.** `make lint`: clean; `make test`: 5403 passed, unchanged (documentation-only).

- **CP5 component 1 of 4 built: orphaned-glue detection (ADR-0046 D2/D6), 2026-08-06 — components 2–4 (near-dup sweep, promotion-wrapping, aggregate cohesion) remain unbuilt.** `suite_quality_governance/cp5/` (`pattern_matching.py`, `models.py`, `orphaned_glue.py`) — Layer 4's first real code, ending `suite_quality_governance/`'s placeholder status. **Location correction, prompt-vs-ADR conflict, ADR wins:** the build brief specified `automation_engineering/cp5/`; ADR-0033 (Accepted, Recommendation 1) already locks Layer 4's own package as `suite_quality_governance/` ("the disambiguated names... effective immediately for new documents"), so the package was built at `suite_quality_governance/cp5/` instead, importing Layer 3's catalog/reuse/embedding modules directly (ADR-0046 D7) rather than living inside Layer 3's own package. **The deterministic gate:** `pattern_matching.py::pattern_matches_text` reproduces Cucumber-JVM's own literal step-binding rule (Cucumber Expression → regex, regex-style patterns via `re.fullmatch`, plain phrases via exact equality) — confirmed this did NOT already exist anywhere in the codebase (the catalog's own `parse_captures` parses a pattern's captures, never matches literal text against it; the reuse engine's matching is semantic/embedding-based, a different question). Reuses the catalog's own pattern classification rather than a second heuristic: `automation_engineering/catalog/alignment.py`'s private `_looks_like_regex`/`_CUCUMBER_EXPRESSION_CAPTURE_RE` were promoted to public `looks_like_regex`/`CUCUMBER_EXPRESSION_CAPTURE_RE` for this reuse. **The semantic advisory:** optional, embedding-based, attached only to an already-orphaned finding, reusing the SAME embedding boundary the reuse engine already uses — `automation_engineering/reuse/embeddings.py`'s private `_cosine_similarity` (`live_matcher.py`) was promoted to public `cosine_similarity` (embeddings.py itself, alongside `EmbeddingProvider`) and `live_matcher.py`'s private `_embedding_text` was promoted to public `step_definition_embedding_text`, exercising ADR-0046 D3/D7's "promote, don't duplicate" requirement ahead of the near-dup sweep (component 2) that originally motivated it — both `live_matcher.py`'s own existing tests (42 total across live-matcher/embeddings/catalog-alignment) re-run and pass unchanged, confirming the refactor is behavior-preserving. Default semantic-hint floor reuses `reuse/engine.py`'s own real, live-calibrated `DEFAULT_GENERATE_FLOOR` (0.70), not a new invented threshold. **Proven, not merely asserted:** referenced-by-current-need → not orphaned; referenced-by-nothing → orphaned (including a Cucumber-Expression-shaped pattern, proving the deterministic matcher — not a naive substring check — actually drives the gate); a semantic hint attached to an orphan never changes CP5's own verdict or orphan set (three parallel runs — no provider, a hint-clearing provider, a hint-missing provider — assert identical verdicts/orphan sets, only the `semantic_hint` field differs); one batched `embed()` call for the whole detection, never one per asset; and — the load-bearing safety proof — an end-to-end run against a REAL filesystem scan (`scanner.reconcile` + `stage.gherkin_needs` derivation, not a hand-built catalog) confirms the tracked step-definition file's content and mtime are byte-for-byte unchanged and no file was created/moved/deleted, proving detection never mutates the baseline (ADR-0046 D2: "flag for review, never auto-remove"). `make test`: 5403 → 5431 (+28: 15 pattern-matching, 13 orphaned-glue); `make lint`: clean; mypy (informational): 432 → 432, no rise, new code (`suite_quality_governance/cp5/`, the two promoted-function refactors, and both new test modules) fully clean under mypy.

- **CP5 component 2 of 4 built: the cross-suite near-duplicate sweep (ADR-0046 D3/D6), 2026-08-06 — components 3–4 (promotion-wrapping, aggregate cohesion) remain unbuilt.** `suite_quality_governance/cp5/near_duplicate_sweep.py`, alongside component 1. **Distinct from ADR-0045 D2(b), proven not merely asserted:** D2(b) is an exact content-hash check at promotion time; this sweep is semantic and retrospective, over the whole reconciled catalog. A dedicated test constructs two step-definition assets with DIFFERENT content-hashes (proving `AssetCatalog.by_content_hash` genuinely would not connect them — D2(b)'s own blind spot) that the sweep still clusters via cosine similarity — the direct proof the two mechanisms catch different things. **Advisory, not gating, structurally, not just by convention:** per ADR-0046 D6's own composition table, `Cp5NearDuplicateSweepResult` carries NO `overall_verdict` field at all (unlike component 1's gating `Cp5OrphanedGlueResult`) — there is no PASS/FAIL for a component that never gates CP5's own verdict; `clusters` alone is the entire output. **Mechanism:** embeds every step-definition's text (reusing component 1's promoted `step_definition_embedding_text`/`cosine_similarity`) in exactly ONE batched `embed()` call for the WHOLE catalog regardless of size — proven directly (a 5-asset catalog produces exactly one call carrying all 5 texts) — then computes every pairwise cosine similarity in memory (O(n²) arithmetic, zero further network cost) and groups qualifying pairs (`>= threshold`, default `0.90`, anchored to the same real `0.8980` paraphrase measurement component 1's floor already cites) into TRANSITIVELY-connected clusters via a small union-find, not merely independent pairs — proven for a 3-mutual case (one cluster, not three pairs) and, more sharply, a transitive CHAIN where the two endpoints never independently qualify (still one cluster of 3, but `pairwise_scores` reports only the two edges that actually qualified, never a fabricated third). Threshold configurability proven directly (the same pair flips flagged/unflagged as `threshold` moves across its score). `embedding_provider` is a REQUIRED parameter here (unlike component 1's optional one) — this component's entire determination is semantic, so there is no honest deterministic-only default; fewer than 2 assets short-circuits before any call is made (proven with a provider that fails the test if ever invoked). **Action, proven:** flag-for-review only — no merge, no modification, no deletion; the same real-filesystem, content-and-mtime-unchanged proof component 1 established is repeated here end-to-end against a real `scanner.reconcile` scan. `make test`: 5431 → 5444 (+13); `make lint`: clean; mypy (informational): 432 → 432, no rise, new code fully clean under mypy.

- **CP5 component 4 of 4 built: aggregate-release cohesion (ADR-0046 D5/D6), 2026-08-06 — component 3 (promotion-wrapping, the capstone composing all four detectors) remains unbuilt; skipped ahead of component 3 per this task's own numbering.** `suite_quality_governance/cp5/cohesion.py` (the gate, mirroring `automation_engineering/cp3/gate.py`'s own composition shape) and `suite_quality_governance/cp5/compile_check.py` (the compile seam, mirroring `automation_engineering/cp3/sonar/{adapter,stub_adapter,live_adapter}.py`'s own `Protocol` + stub/live split). Two DETERMINISTIC criteria, both gating (ADR-0046 D6 — unlike component 2's advisory sweep): `compiles` and `no_ambiguous_glue`. **Compile boundary, structurally proven, not merely documented:** `LiveCompileChecker` invokes exactly one Maven goal, `test-compile` (compiles test sources, never executes them) — a dedicated test asserts the literal string `"test"` never appears in the constructed argv, and that no execution/SUT-shaped flag (`verify`, `integration-test`, `selenium`, `webdriver`, `surefire:test`) ever does either. Mirrors CP3's Sonar adapter's own "adapter mechanics are fake-tested" discipline (`subprocess.run` mocked in every automated test — no machine running `make test` is assumed to have `mvn`/a JDK installed). **Unlike CP3's Sonar adapter (never exercised live — no server was reachable), a real Maven/JDK toolchain IS reachable in this environment, and was used for a genuine, one-time, manually-run live proof (not part of the automated suite):** `mvn test-compile` against the REAL tracked `test-suite-baseline/pom.xml` returns exit code 1 — **the tracked baseline does not currently compile** (all 34 step-definition classes reference `com.automation.pages` page-object classes that do not yet exist in the tracked baseline) — independent evidence the `compiles` criterion is a meaningful check, not a well-typed no-op; confirmed to write only to the already-`.gitignore`d `target/` directory (`git status --porcelain` clean immediately after, `target/` reported `!!`/ignored). Recorded in `suite_quality_governance/README.md`; `target/` removed after verification. **Ambiguous-glue v1 (exact pattern-string collision across distinct classes)** mirrors `automation_engineering/cp4/gate.py::_evaluate_duplicates`'s own cross-asset grouping shape exactly — proven: two DIFFERENT classes sharing one exact pattern flags (naming both classes); the SAME class repeating a pattern does NOT flag here (CP3's own `duplicate_steps` criterion, ADR-0044 D5, is the correct owner of that instead); two DIFFERENT patterns (including a same-shape-but-different-Cucumber-Expression-type pair, `{string}` vs `{word}`) do NOT flag, proving v1's own exact-collision boundary and that full Cucumber-Expression overlap detection is genuinely deferred, not silently built. Composed verdict proven independently for all four cases (both pass, compile-only-fail, glue-only-fail, both fail). No tracked-baseline mutation from the ambiguous-glue scan proven end-to-end against a real `scanner.reconcile` scan carrying a genuine collision (content/mtime/tree unchanged), the same proof components 1–2 already established. `make test`: 5444 → 5468 (+24: 9 compile-check, 15 cohesion); `make lint`: clean; mypy (informational): 432 → 432, no rise, new code fully clean under mypy.

- **CP5 component 3 of 4 built — THE CAPSTONE, promotion-wrapping (ADR-0046 D4), 2026-08-06 — CP5 IS NOW COMPLETE, all four components built.** `suite_quality_governance/cp5/promotion_wrap.py::evaluate_promotion_wrap` composes components 1 (`detect_orphaned_glue`), 2 (`sweep_near_duplicates`), and 4 (`evaluate_cohesion`) into ONE suite-level verdict wrapping ADR-0045's per-asset promotion — exactly what ADR-0045 D1 anticipated ("Layer 4, once built, may wrap this promotion decision in richer suite-level governance"). **DECISION 1 (composition) — resolved by the ALREADY-ACCEPTED ADR-0046 D6, not invented, surfacing a real prompt-vs-ADR conflict per this task's own instruction ("the ADR wins"):** the build brief's own pre-flight framing suggested orphaned-glue might be "arguably flag-for-review, not a hard block" — but D6's own composition table, frozen before this task began, states plainly that "a deterministic-check failure (orphan-by-pattern, exact glue collision, compile failure) MAY GATE CP5 DIRECTLY" — naming orphan-by-pattern explicitly alongside cohesion as a hard gate, not a soft one. `overall_verdict` is therefore `PASS` iff BOTH `cohesion.overall_verdict` AND `orphaned_glue.overall_verdict` are `PASS`; `near_duplicates` never participates in that computation, however many clusters it finds — proven directly: an orphan alone (cohesion clean) fails the whole capstone; a near-dup cluster alone (cohesion and orphan both clean) leaves `overall_verdict` `PASS` but sets `has_review_worthy_findings = True`. **DECISION 2 (the pre-existing compile failure) — resolved as an ABSOLUTE gate plus honest, non-gating attribution, not an unauthorized delta gate:** ADR-0046 D5's own text locks "does the assembled suite compile," a check on the suite as assembled, not "did this promotion's own delta make it worse" — no delta-gating language exists anywhere in the Accepted ADR, so none was built. Component 4's own live finding (the tracked baseline does not currently compile) is therefore a genuine, correct suite-reject under this gate, not a bug. What was added instead: `CompileAttribution` (`.models`) — whenever `compiles` fails, its error messages (which carry real file paths) are checked against the paths this run's promotion just staged, producing a `detail` string telling a reviewer whether the failure involves a just-staged file or is an entirely pre-existing baseline condition. Enrichment only — proven directly that it never changes the gate (`overall_verdict` stays `FAIL` in both the "involves staged file" and "pre-existing, does not involve any staged file" cases; `None` when compile passes; a distinct "no files were staged this run" message when `staged_paths` is empty). **Wrap ordering, proven not asserted:** a real filesystem test writes a pre-existing step-def file plus a second file mirroring what per-asset promotion would have JUST staged (writing directly, since `reconcile()` reads the working tree regardless of git index state — the same property the loop-closing promotion proof already established); the capstone's own single internal `reconcile()` call is shown to see BOTH, evidenced by the newly-"staged" file's own step correctly appearing as an orphan finding. **Read-only, always:** the module calls no git command at all (`git add`/`commit`/`restore` never appear); a suite-reject leaves whatever per-asset promotion already staged completely untouched — proven via the same real-fs content/mtime/tree-unchanged proof components 1/2/4 already established. **Routing — no new queue:** `Cp5PromotionWrapResult` IS the routing record, mirroring `automation_engineering.reuse.engine`'s own "a structured record... no separate queue, no new mechanism" discipline exactly; no `Escalation`/`PromotionEscalated` object is constructed (neither fits a suite-level composed verdict, the same reasoning components 1/2 already used for their own finding shapes). **Per-asset promotion genuinely unchanged:** no file under `automation_engineering/promotion/` was touched by this task; the full suite (including every existing promotion test) passes unchanged. `make test`: 5468 → 5480 (+12); `make lint`: clean; mypy (informational): 432 → 432, no rise, new code fully clean under mypy. **CP5 is now complete** — the remaining Layer 4 work is CP7/CP8 (ADR-0046 D8, named/scoped only) and wiring CP5 into a live run-state stage (not started).

## 4. Open questions this baseline does not resolve (consolidated from the TBD column)

1. **Sharpened by ADR-0042 Decision 7, engaged (not resolved) by ADR-0043, still open.** Where does ADR-0020's Layer 2.5 (Executable Specification Engineering, CAP-087, Proposed, no code) fit under ADR-0031's model? Evaluated against ADR-0031's Layer 2 (Feature Engineering) and found **materially different**, not a naming collision: (a) CAP-087's own frozen dependency boundary consumes `RequirementEnhancementResult`/`GroundingResult`/`ValidationResult`/`RecommendationResult`/`LearningResult` directly — exactly the Layer-1-internal artifacts ADR-0034 Recommendation 1 forbids anything outside Layer 1 from depending on, bypassing the `TestableRequirementSet` boundary ADR-0034/ADR-0042 froze; (b) CAP-087's output is a renderer-agnostic Specification Model (Cucumber only "the first reserved renderer target"), while ADR-0031's Layer 2 is unconditionally Cucumber-committed. ADR-0043 (Layer 2's own architecture-freeze ADR, per ADR-0042 Recommendation 4's requirement to engage, not merely cite) confirms both conflicts are unchanged by its own D1–D8: Layer 2 stays `TestableRequirementSet`-only and unconditionally Cucumber-committed. **Still open** — a CAP-087-specific revision, or a future amendment to ADR-0043, is what would actually resolve it.
2. ~~What is `TestableRequirement`'s and `TestableRequirementSet`'s exact field list, hash algorithm, and `supersedes`-detection mechanism?~~ **Field list and hash algorithm resolved by [ADR-0042](../adr/0042-testablerequirement-field-specification.md).** The `supersedes`-detection mechanism remains open — ADR-0042 Decision 5 records it as blocked on a cross-run requirement index ADR-0036's filesystem-only model does not provide; no implementation may invent one before a future ADR resolves this.
3. ~~Is `canonical_requirement.py`'s `SourceRef` salvaged into `TestableRequirement`, or discarded with the rest of the file?~~ **Resolved by [ADR-0042](../adr/0042-testablerequirement-field-specification.md) Decision 4**: salvaged, using its actual `system`/`external_id`/`url` shape. Blocks one item of ADR-0035's removal list no longer — that item is unblocked (not yet executed).
4. What is the exact `run_id` scheme, `run_state.json` schema, and lockfile mechanism? Deferred to ADR-0036's future implementation.
5. Does Layer 7 (Governance Dashboard) genuinely participate in the per-run stage sequence, or is it a separate cross-run service? Structurally unresolved by ADR-0036 §D5.
6. ~~What is the concrete Java stack (build tool, Java version, Cucumber-JVM version, runner, browser-automation and assertion libraries, reporter) for the generated suite?~~ **Resolved by [ADR-0041](../adr/0041-java-stack-for-the-generated-test-suite.md)**: Maven, Java 21, BOM-managed JUnit/Cucumber versions, JUnit Platform Suite runner, JUnit Jupiter–only assertions, Selenium 4.25.0 (Selenium Manager, no WebDriverManager), three-reporter split. ~~ADR-0037's tracked-baseline-path TBD~~ **resolved:** `test-suite-baseline/` at repo root (additive note in ADR-0037, 2026-07-25). ~~Its day-one base-framework-shape TBD~~ also **resolved as realized, not frozen:** `BasePage`/`DriverFactory`/`Hooks`/`ConfigReader` (additive note in ADR-0037, 2026-07-25) — extension expected as Layer 3 generates further code. ~~Only ADR-0037's promotion review mechanism remains open.~~ **Resolved by [ADR-0045](../adr/0045-scoped-asset-promotion.md)** (2026-07-30): promotable gate, review model, and capstone sequencing locked; only the promotion mechanism's exact git mechanics remain open, deferred to implementation (see item 14(d) below).
7. Is ADR-0039 (Jenkins, execution backend) ratified? It is Proposed only — nothing in Layer 4 or Layer 5's future architecture may be built against it until it is Accepted.
8. How does an asynchronous, Jenkins-delegated Layer 5 stage fit the otherwise-synchronous stage model ADR-0036 defines? Explicitly left to Layer 5's own future architecture-freeze ADR (ADR-0039 §D2).
9. When does ADR-0032's Layer 1 freeze actually lift? **Not by ADR-0043** — its precondition 1 (Layer 2 at Runtime Integration) is not met by an architecture-freeze ADR with zero implementation (ADR-0043 D8). Left to a separate, future, numbered ADR, proposed only once Layer 2's implementation (independently authorized by ADR-0043 itself) reaches Runtime Integration, naming which Layer 1 CAP number(s) it authorizes.
10. ~~Does ADR-0014's prompt registry's extraction to a shared platform-level location (ADR-0043 D4, for Layer 2 reuse) fit within ADR-0032's five enumerated carve-outs, or does it require the freeze to be properly lifted first?~~ **Resolved (2026-07-27): carve-out-3 reading.** The forcing clause fired when Layer 2's prompt-conversion work first needed to import the registry. Resolved as a no-behavior-change relocation (`requirement_intelligence/prompts/{framework,models}` → `shared/prompts/{framework,models}`), not a freeze-lift precondition — see ADR-0043 D4's own resolution note and §3 above.
11. What is the step-vocabulary index's exact storage format and matching algorithm (ADR-0043 D6)? What is the test-data specification's exact field shape (ADR-0043 D7)? What is the Python Gherkin-lint port's exact package structure (ADR-0043 D3)? What is CP2's exact acceptance-criteria-coverage threshold (ADR-0043 D5)? All locked in principle by ADR-0043, deferred to Layer 2's own implementation for their concrete shape.
12. **New (2026-07-27), from the prompt-registry relocation — split into two halves, resolved separately (2026-07-29).** Two Layer-1-shaped elements moved to `shared/prompts/` unchanged, deliberately not generalized: (a) ~~`ARTIFACT_CONTEXT_PLACEHOLDER = "{artifact_context}"` in `prompt_template_contract.py` — named for Layer 1's `ConsolidatedArtifact` injection specifically; whether Layer 2's own prompt assembly reuses this contract under the same placeholder name, or the constant is generalized/parameterized, is undecided.~~ **DONE (2026-07-29):** renamed to `CONTEXT_PLACEHOLDER` — the Python symbol only, a pure rename; the placeholder's *value* (`"{artifact_context}"`, the literal token every already-committed governed template embeds) is unchanged, since changing it would break every registered template. Every reference updated (`shared/prompts/framework/prompt_template_contract.py`, `shared/prompts/framework/__init__.py`, `requirement_intelligence/prompts/__init__.py`, `requirement_intelligence/tests/unit/test_prompt_template_contract.py`); no string/dotted-path reference existed to miss. Whether Layer 2's own prompt assembly ever reuses this contract at all remains a separate, still-open design question the rename does not touch. (b) `PromptCompatibility`'s five fields (`normalization_version`/`validation_version`/`cp1_version`/`golden_dataset_version`/`output_schema_version`) are hardcoded to Layer-1 subsystem names. ~~**DEFERRED to Layer 3, not generalized here (2026-07-29):** generalizing now, against only two consumers — Layer 1 genuinely using all five fields, Layer 2 with nothing to declare against any of them — risks converging on a Layer-1-shaped-minus-Layer-2 model, not a genuinely general one. **Trigger:** generalize when Layer 3's own prompt registration needs its own compatibility dimensions declared — a third, structurally different consumer, giving three real data points instead of two to design the generalization against. **Trigger fired (2026-07-29, ADR-0044):** Layer 3's architecture is now locked and it is a real, imminent third registry consumer (additive note in ADR-0043 D4) — the generalization itself remains undone, deferred to Layer 3's implementation, at the point its first prompt actually registers.~~ **DONE (2026-07-31, the Layer 3 step-definition generator build):** generalized to a single `dimensions: dict[str, str]` mapping (`shared/prompts/models/prompt_compatibility.py`), designed against all three real consumers at once — Layer 1 keeps its original five dimensions unchanged in name and value; Layer 2 now declares only `output_schema_version` and no longer fabricates the other four as `"n/a"`; Layer 3's `generate_step_definitions` prompt declares its own `output_schema_version`/`customqa_profile_version`, neither Layer-1-shaped. A mapping was chosen over per-layer subtypes specifically because a subtype design would re-derive the same per-consumer-shape problem for every future consumer; see the model's own docstring and the additive note on ADR-0043 D4 for the full account. `make test` 5067 → 5070 (+3), `make lint` clean, mypy no rise.
13. **New (2026-07-27), from the D2/D5 tag-assignment-ordering resolution.** ~~Implementation TBD~~ **IMPLEMENTED (2026-07-29), construct-tested, never observed live.** A remediation-created (split) scenario's `SCN-*` is now platform-minted fresh, via the existing content-addressed mechanism (`contracts.id_generation.generate_scenario_ids`), never LLM-chosen or copied from the scenario it split from; a preserved scenario keeps its existing id — identity is TAG-based (per `fix_gherkin_lint`'s own "preserve every tag exactly as given" contract), not content-based, since a scenario's content legitimately changes under an ordinary remediation (e.g. a rename fixing `no-dupe-scenario-names`) without becoming a different scenario. Built in `feature_engineering/remediation/loop.py`'s `_remint_split_scenario_ids`, wired into `run_cp2_remediation`'s Tier-2 attempt loop; covered by `tests/unit/test_feature_engineering_remediation.py`'s `TestSplitScenarioRemint` (3 tests: survivor-keeps-id/new-gets-a-fresh-deterministic-id, cross-run determinism, and the no-split byte-identical no-op). **Honest status, not overstated:** two live runs (60 generations) observed **zero** scenario splits — this path is exercised only against a CONSTRUCTED split (a `StubFeatureRemediator` scripted to split a scenario, deliberately including the dangerous case of copying the original id onto both resulting halves), never against real live-model split behavior, because none has ever occurred. Correct to build defensively (rare is not never; an unhandled split would silently orphan an id) — but the status is *implemented, construct-tested, never observed live*, not *proven*.
14. **New (2026-07-29), from ADR-0044 (Layer 3 Automation Engineering Architecture Freeze) — deferred to implementation, each with a stated trigger.** (a) The asset catalog's (D3) exact storage location relative to the tracked `test-suite-baseline/` and the untracked per-run workspace — resolve when the catalog is first built against a real filesystem layout. (b) Semantic-match mechanism, embeddings vs. a per-step LLM call (D3) — ADR-0044 records a lean toward embeddings (cheaper, batchable, cacheable, reduces the free-tier 429 exposure the Layer 2 measurement already exposed), not a lock; resolve at implementation. (c) ~~The test-data specification → Java mapping (D7) — resolve once the registered `generate-test-data` prompt's output is reconciled field-by-field against ADR-0043 D7's specification.~~ ~~**Partially resolved (2026-07-31, the Layer 3 test-data generator build):** `generate_test_data` v1.0.0 is now actually registered (it never was before this build — the ADRs' own "already-registered" language described only the original POC's prompt catalog, not this platform's governed registry), and `TestDataSpecification`/`TestDataFieldSpec` (`automation_engineering/generation/models.py`) map field-for-field onto D7's own prose contract. **Still open:** this reconciliation is against the CONTRACT D7 defines, not a real Layer 2 emission — Layer 2 implements no test-data specification emission today (a repo-wide search for `TestDataSpec`/`test_data_spec`-shaped code in `feature_engineering/` returns nothing), so the real end-to-end mapping (an actual Layer 2 output reconciled against this shape) remains unresolved until Layer 2 builds that emission. Resolve when Layer 2 does.~~ **FULLY RESOLVED (2026-07-31, the Layer 2 test-data specification emission build).** Layer 2 now implements D7's own locked-but-never-built decision: `feature_engineering/stage/test_data_spec.py::build_test_data_specification` derives one `TestDataSpecification` per requirement from `AcceptanceCriterion.data_fields[]`/`.polarity_hints[]` (both real, committed ADR-0042 fields, verified directly: `data_fields[]`'s own field-table entry already says "Seeds `Examples:` tables and Layer 3's test-data spec"), rolling up per-field variant unions across every criterion. The shape itself was RECONCILED, not merely reused: it moved from `automation_engineering/generation/models.py` (Layer 3's own unilateral definition) to `contracts/test_data_specification.py` — the SAME shared home `TestableRequirement` itself uses for its own Layer 1 → Layer 2 boundary — stripped of the two Java-shape fields (`target_class_name`/`target_package`) Layer 2 could never have honestly supplied; Layer 3's orchestrator now derives those itself (`derive_test_data_class_name`). **Finding, load-bearing and recorded honestly:** neither `data_fields` nor `polarity_hints` is populated by any real Layer 1 emitter code today (`requirement_intelligence/testable_requirement/emitter.py` never passes either when constructing `AcceptanceCriterionInput`) — confirmed against the real, committed `output/latest/testable_requirement_set.json` (30 requirements, 30 acceptance criteria, every one `polarityHints: []`/`dataFields: []`). Every specification Layer 2 derives from the real saucedemo corpus today is therefore honestly EMPTY — the derivation logic itself is proven correct (against hand-constructed, schema-compliant criteria carrying real hints, in `test_feature_engineering_test_data_spec.py`), it simply has nothing to derive from yet on live output; this is Layer 1's own gap, not this build's defect, and is now the honest state of the pipeline rather than an unbuilt one. **THE END-TO-END LINK is proven directly**, real Layer 2 output feeding the real Layer 3 seam with no adapter (`tests/unit/test_layer2_layer3_test_data_end_to_end.py`): once against a hand-built, `data_fields`-populated requirement (producing real fields), and once against every one of the real corpus's 30 requirements (producing the honestly empty case, all 30, zero exceptions). Wired into stage 14's own artifact set (`test_data_specifications.json`, alongside `feature_engineering_package.json`/`traceability.json`, `.runner.run_feature_engineering_stage`) — additive, not a new stage. `make test`: 5238 → 5251 (+13); `make lint`: clean; mypy (informational): 432 → 432, no rise (`git stash -u` baseline re-verified directly, zero diff in error content). (d) ~~The promotion mechanism, workspace → tracked baseline (ADR-0037's own long-open TBD — item 6 above) — unresolved by ADR-0044, but now additionally motivated: promotion is also how the reuse catalog (D3) actually grows across runs, not only how a workspace asset becomes permanent.~~ **Decision resolved by [ADR-0045](../adr/0045-scoped-asset-promotion.md) (2026-07-30):** promotable gate (CP2+CP3+CP4 pass and confirmed non-duplicate, reusing ADR-0044 D3/D4's own identity mechanism), review model (auto-promote on clean pass; ADR-0044 D4 escalations get the same human review, not a second queue), and sequencing (Layer 3's capstone build step) are all locked, pulled forward from a future Layer 4 build. ~~**Still open, TBD-at-implementation:** the promotion mechanism's exact git mechanics (auto-commit vs. stage-for-review).~~ **RESOLVED (2026-08-02, the Layer 3 promotion build — see §3 below): stage-for-review.** `automation_engineering/promotion/mechanism.py::stage_promoted_assets` runs `git add` on every promoted file and never calls `git commit` — the promotion DECISION (D2/D3: is this asset safe to move into the baseline) and the resulting GIT HISTORY (D5: does that move become a commit without a human looking at the diff) are answered separately; auto-promoting a clean asset does not imply auto-committing it. Proven not to break the reuse loop: `reconcile()` reads the working tree directly, so a staged-but-uncommitted promoted asset is already visible to the very next run's catalog reconciliation (`tests/unit/test_automation_engineering_promotion_loop.py`).
15. **New (2026-07-31), surfaced by item 14(c)'s own resolution — a Layer 1 emission gap, distinct from the now-resolved Layer 2 emission gap.** The test-data pipeline is fully built and proven correct end-to-end (item 14(c) above: Layer 2's `build_test_data_specification` derives a `TestDataSpecification` from `AcceptanceCriterion.data_fields[]`/`.polarity_hints[]`, Layer 3's `generate_test_data_class` consumes it, the link is proven with real code on both sides), but it produces only honestly EMPTY specifications on the real saucedemo corpus, because Layer 1's own emitter never populates the two fields the whole pipeline depends on. Verified directly, not assumed: `requirement_intelligence/testable_requirement/emitter.py` constructs every `AcceptanceCriterionInput(category=category, statement=statement)` (line 247) with no `polarity_hints`/`data_fields` argument, so both take `AcceptanceCriterionInput`'s own defaults (`contracts/testable_requirement.py`, both `= ()`); the real, committed `output/latest/testable_requirement_set.json` confirms the effect on live output — 30 requirements, 30 acceptance criteria, every one `polarityHints: []`/`dataFields: []`. [ADR-0042](../adr/0042-testablerequirement-field-specification.md)'s own field table already documents the *intent* (`data_fields[]`: "Seeds `Examples:` tables and Layer 3's test-data spec") without ever specifying *population* — the field was locked as a contract slot, not as emitter behavior, and no Layer 1 code was ever built to fill it. **The full dependency chain, recorded so it stays visible:** Layer 1 analysis must produce the underlying positive/negative/boundary and data-field signal → the emitter must populate `AcceptanceCriterionInput.polarity_hints`/`.data_fields` from it → Layer 2's `build_test_data_specification` (already built) derives a non-empty specification → Layer 3's `generate_test_data_class` (already built) generates a non-trivial class. Only the first two links are missing. **Owner:** Layer 1 (`requirement_intelligence/testable_requirement/emitter.py`, and whatever upstream analysis step would first produce the signal to populate it from). **Trigger to resolve:** whenever Layer 1 implements emission of this signal — a separate, future build, out of scope for both the now-resolved item 14(c) and the Layer 2 spec-emission work that surfaced this gap.

16. **New (2026-08-04), from the Layer 1-3 end-to-end integration run — two exception-handling gaps in the CLI's live pipeline path, verified directly against `scripts/run_requirement_analysis.py` and `feature_engineering/stage/runner.py`.** (a) A live-provider rate-limit failure during stage 14 aborts the whole stage instead of being recorded as one escalated requirement. `feature_engineering/generation/assembler.py::generate_feature_file` (line 191) calls `content_generator.generate(requirement)` unguarded; `run_feature_engineering_stage`'s own per-requirement loop only catches `FeatureGenerationError` (`feature_engineering/stage/runner.py`, the `except FeatureGenerationError as exc:` block). `LiveFeatureContentGenerator` raises a different, sibling exception class, `LiveGenerationError` (`feature_engineering/generation/live_content_generator.py:59`, "a provider exception... a non-`COMPLETED` normalized execution outcome... an empty/whitespace-only response"), which is not a subclass of `FeatureGenerationError` and is therefore never caught there — it propagates out of `run_feature_engineering_stage` to `execute_feature_engineering_stage`'s outer `except Exception as exc: run_state_mgr.fail_stage(STAGE_ID, error=exc)` (`runner.py`, `execute_feature_engineering_stage`), which fails the entire stage. Reproduced live: a single 429 `RESOURCE_EXHAUSTED` on the first of 20 requirements failed stage 14 outright with zero requirements processed, rather than escalating that one requirement and continuing. (b) `--resume` cannot resume a failed Layer 2 stage. The CLI's own "already complete" check (`scripts/run_requirement_analysis.py`, lines 940-953) inspects only `stage_id == "execution_package_write"` (Layer 1's own final stage) — confirmed by the code's own adjacent comment ("the one practically skippable unit today... per-stage skip is latent capability, not exercised by this CLI"). A run whose `feature_engineering` stage sits at `status: failed` in `run_state.json` is reported by `--resume <run_id>` as "already complete and unchanged -- nothing to do," silently ignoring the failure. **Both gaps were MASKED in every prior measurement run** (e.g. the 30/30-clean baseline in `docs/architecture/architecture-baseline-v2.md`'s own history and the earlier CAP-076 series measurements) because those were driven by a resume-aware wrapper script external to the raw CLI, not by `analyze --resume` itself — the raw CLI path was not exercised end-to-end live, under real rate-limit pressure, until this integration run. **Owner:** the CLI's live-pipeline path (`scripts/run_requirement_analysis.py::handle_analyze`, Layer 2 failure/resume handling). **Trigger to resolve:** whenever stage 14 (or a future stage) is next hardened for unattended live operation — either (a) `run_feature_engineering_stage` catching provider-boundary exceptions per-requirement the same way it already catches `FeatureGenerationError`, or (b) `--resume`'s skip-check inspecting every declared stage, not only `execution_package_write`, before either is relied on for an unattended live run.

    **RESOLVED (2026-08-04, the CLI live-pipeline robustness build — both (a) and (b) fixed, no Layer 2 business logic changed).**

    **(a), F1 — fixed by escalate-immediately, not retry.** A new shared, seam-level exception, `feature_engineering.generation.errors.TransportFailureError`, was introduced; `LiveGenerationError` and `LiveRemediationError` (`feature_engineering/remediation/live_remediator.py`) now subclass it instead of bare `Exception` — a re-parenting, not a behavior change to either class's own failure modes or message text. `run_feature_engineering_stage` (`feature_engineering/stage/runner.py`) now catches `TransportFailureError` at BOTH call sites that were unguarded — the initial `generate_feature_file` call, and (found and fixed symmetrically, since it carried the identical unguarded pattern, never observed live because live remediation itself is rare) the `run_cp2_remediation` call in the D5 remediation loop. Each catch records the affected requirement as an escalated `FeatureRecord` (`escalated=True`, `remediated=False`, `feature_path=None`) with `escalation_reason` prefixed `"transport failure (no retry attempted): ..."` — deliberately distinct from a content failure's `"pre-assembly generation contract violation: ..."`, preserving the taxonomy a human reviewer needs (provider/network conditions vs. generated-content problems). **Decision, stated explicitly:** escalate immediately, no retry — the platform has no retry/backoff anywhere in this call path today (unchanged by this fix, `live_content_generator.py`'s own docstring still says so), and adding one was explicitly out of this fix's scope. **Deferred follow-up, tracked, not built:** bounded transport retry-with-backoff at this seam (the pacing a wrapper script did manually during the integration run) remains a legitimate future feature, to be built deliberately as its own change, not smuggled into this abort-bug fix.

    **(b), F2 — fixed narrower than "every LIVE_STAGE_IDS stage" (tried, reverted).** The whole-run "already complete" check in `scripts/run_requirement_analysis.py` (`handle_analyze`) now also requires `feature_engineering` to have reached `SUCCEEDED`/`SKIPPED` (or, for a run whose `testable_requirement_emission` never succeeded — e.g. `--dry-run` — that check is skipped entirely, since such a run was never going to reach stage 14 to begin with), alongside the existing, unchanged `execution_package_write` freshness check. Initially implemented against every `LIVE_STAGE_IDS` stage (L1 + L2) and reverted after writing the F2 regression test: several Layer 1 tail phases (`requirement_enhancement`, `recommendation`, `continuous_improvement`, `knowledge_graph`, `organizational_memory`, `learning`) are individually best-effort by this platform's own existing, documented design (each phase's own docstring: "a failure here is surfaced but never fatal to the analysis run") — requiring ALL of them SUCCEEDED would make `--resume` needlessly re-run a run that had already legitimately finished, the opposite failure mode from the one this fix targets. `execution_package_write` reaching `SUCCEEDED` already reflects that every phase was attempted; only `feature_engineering`'s own status was the genuinely missing signal. This is a CLI wiring fix only — `RunStateManager` already recorded every stage's real status (confirmed: `first_non_succeeded_stage_id()` already existed, unused by the CLI, and was NOT reused here either, since it walks ADR-0036's full 19-stage table including the permanently-`PENDING` Layer 3-7 placeholders — using it directly would mean `--resume` never reports "complete" for any run today, a different bug in the opposite direction).

    Verified: `tests/unit/test_feature_engineering_stage.py::TestTransportFailureEscalation` (3 tests — one requirement's transport failure escalates while the rest of the batch completes normally and the stage stays `SUCCEEDED`; a transport failure and a content failure in the same run escalate with different, distinguishable reasons; a remediation-phase transport failure escalates symmetrically) and `tests/unit/test_run_requirement_analysis.py::test_resume_detects_a_failed_feature_engineering_stage_and_resumes_it` (a run whose `feature_engineering` genuinely `FAILED` is detected as not-complete by `--resume` and the stage is re-attempted and succeeds, rather than being reported "already complete"). Regression: the existing all-clean `--resume` test (`test_stage14_resume_skips_layer1_and_skips_unchanged_stage14`) still passes unchanged. `make test`: 5324 → 5328 (+4); `make lint`: clean; mypy (informational): 432 → 432, no rise, re-verified directly via `git stash -u` both before and after this build, not assumed from memory.

17. **New (2026-08-04), from the Layer 1-3 end-to-end integration run — a portability gap in the CP3 Sonar adapter, verified directly against `automation_engineering/cp3/sonar/live_adapter.py`.** `LiveSonarQualityGateAdapter.submit_scan` invokes Maven with the short goal-prefix form: `["mvn", "-q", "-f", str(project_root / "pom.xml"), "sonar:sonar", f"-Dsonar.host.url={self._base_url}", f"-Dsonar.projectKey={project_key}"]` (`live_adapter.py`, `submit_scan`). Maven only resolves an unqualified `sonar:sonar` goal against plugin groups a `~/.m2/settings.xml` explicitly registers (the default groups are `org.apache.maven.plugins`/`org.codehaus.mojo`; `org.sonarsource.scanner.maven` is neither). Reproduced live: on a machine with no `~/.m2/settings.xml` at all, this exact argv fails with `exit 1`, `"No plugin found for prefix 'sonar'"` — a `SonarScanError` this build's own `_evaluate_sonar_gate` (`automation_engineering/cp3/gate.py`) correctly turns into a FAIL criterion (CP3 fails closed, as designed), but the failure is an environment-portability gap, not a code-quality signal about the scanned Java. Adding a `pluginGroups` entry for `org.sonarsource.scanner.maven` to `~/.m2/settings.xml` resolves it (verified: submit → poll → quality-gate-fetch then completes end-to-end) — this is a local environment fix this integration run applied and disclosed, not a repository or Sonar-server change, and it is not committed anywhere. **Owner:** the CP3 Sonar adapter (`automation_engineering/cp3/sonar/live_adapter.py`). **Trigger to resolve:** either document the `pluginGroups` registration as a stated operational prerequisite (alongside ADR-0041's already-named JDK 21/Maven prerequisites) or switch `submit_scan` to the fully-qualified goal (`org.sonarsource.scanner.maven:sonar-maven-plugin:sonar`), which needs no plugin-group registration on any machine.

    **RESOLVED (2026-08-05, F3, the stage-15 wiring build).** `submit_scan` now invokes the fully qualified goal (`org.sonarsource.scanner.maven:sonar-maven-plugin:sonar`, `automation_engineering/cp3/sonar/live_adapter.py`'s own `_SONAR_GOAL`) instead of the short `sonar:sonar` prefix — no `pluginGroups` registration required on any machine. Proven: `tests/unit/test_automation_engineering_cp3_sonar_live_adapter.py::test_submit_scan_uses_the_fully_qualified_sonar_goal` asserts the exact argv, mocked (no live Maven process, consistent with this adapter's own existing fake-tested discipline). This stage-15 build is the first to actually construct a `LiveSonarQualityGateAdapter` from the CLI (item 20 below), so the portability gap this item flagged would otherwise have fired on the very first real invocation.

18. **New (2026-08-04), from the Layer 1-3 end-to-end integration run — the `customqa:*` profile is specified but was never created as an artifact.** Full verification and the operational consequence are recorded as additive notes directly on the two ADRs that specify this profile, not duplicated here: ADR-0037 Recommendation 3 (`docs/adr/0037-generated-test-suite-target-and-sut-binding.md`, "Gap note, additive, 2026-08-04") and ADR-0044 D5 (`docs/adr/0044-layer-3-automation-engineering-architecture-freeze.md`, "Gap note, load-bearing, additive, 2026-08-04"). Summary: no `customqa:*` profile export exists anywhere in this repository, and no SonarQube server (including the one this integration run verified as reachable, authenticated, and submission-capable) has ever had one imported — the live `Automation-POC` project's Java profile is the built-in `Sonar way`. ADR-0044 D5's "born compliant, verified compliant" story (constrain at generation, verify at the gate) is therefore currently incomplete: the gate that exists can only verify against the wrong rule set. **Owner:** shared between ADR-0037 (the profile's versioned-artifact existence) and ADR-0044 D5 / the CP3 Sonar adapter (the profile's server-side installation and the gate that evaluates against it). **Trigger to resolve:** the `customqa:*` profile is authored as a real, versioned artifact under the tracked baseline and imported into whatever SonarQube server CP3 targets, in every environment claiming to exercise CP3 end-to-end.

19. **New (2026-08-04), from the Layer 1-3 end-to-end integration run — a correction to that run's own report, recorded here because the run's initial framing was imprecise.** The run's evidence showed one live-selected Engineering Context with `functional=25, security=0, quality=25` and initially characterized this as "a live reproduction of the already-tracked CAP-074B/CAP-076 consolidation-grouping behavior." Re-verified directly against `engineering_context.json`'s own `coverage`/`evidenceBudget` blocks before recording, per this task's own instruction not to inherit the run report's word: that characterization overstates it. CAP-074B's original defect (`docs/reviews/cap-074b-requirement-evidence-investigation.md`, `docs/proposals/cross-source-consolidation-and-selection.md`, Status: Proposal, not implemented) was that 0 of 47 consolidated groups were ever multi-source and the CLI always selected Sonar-only evidence. This run's selected context is genuinely multi-source (`ruleSatisfied: true`, `allPresentCategoriesRepresented: true`, `coverageMode: all_present_categories`, `selectionStrategy: coverage_guaranteed` — `DefaultOrchestrationPolicy`, per ADR-0015's own design) and `security=0` is not a selection failure: the `coverage` block's own `security` domain entry reads `"candidateArtifactCount": 0, "reason": "No candidate group carried security evidence."` — no ZAP-sourced candidate existed for the selected subject (`BadLoginPage.java`) in the first place, and the policy transparently reports that absence rather than silently dropping available evidence the way the pre-CAP-076 defect did. **This is the CAP-076D fix confirmed behaving correctly on live data, not a bug reproduction.** The one genuine nuance worth tracking: CAP-076D's own "all three domains reach Gemini" claim is conditional on domain candidates existing for the selected subject — it is not an unconditional guarantee that every live run's evidence will span JIRA, ZAP, and SonarQube. No code or documentation change is needed; this item exists so a future reader does not mistake this run's `security=0` for a regression. **Cross-references:** `docs/reviews/cap-074b-requirement-evidence-investigation.md` (CAP-074B root cause), `docs/proposals/cross-source-consolidation-and-selection.md` (the still-unapproved consolidation-grouping proposal CAP-074B produced), ADR-0015 (CAP-076B, Engineering Context Orchestration — the actually-shipped fix, at the selection layer, not the consolidation-grouping layer the proposal targets).

20. **New (2026-08-04), from the Layer 1-3 end-to-end integration run — Layer 3 has no live entry point; this is the run's headline finding and the next build.** Layer 1 is reachable via CLI (`scripts/run_requirement_analysis.py`) and API (`app/api/router.py`). Layer 2 is wired into the same CLI as stage 14 (`execute_feature_engineering_stage`, exercised live in this run). Layer 3 (`automation_engineering/`) has six independently well-tested subsystems — catalog (`catalog/scanner.py::reconcile`), reuse (`reuse/engine.py::decide_reuse`), generation (four independent orchestrator functions, one per asset kind, no whole-feature entry point), CP3 (`cp3/gate.py::evaluate_cp3`), CP4 (`cp4/gate.py::evaluate_cp4`), promotion (`promotion/outcomes.py::promote_outcome` + `promotion/mechanism.py::apply_promotion`/`stage_promoted_assets`) — but **nothing chains them.** Verified directly: `app/api/router.py` line 21 is a commented-out `# api_router.include_router(automation_engineering_router)`; no CLI subcommand imports `automation_engineering`; run-state stage 15 (`automation_engineering`, per ADR-0036) sits at `status: pending` in every run's `run_state.json`, always, including this one; `automation_engineering/README.md` still reads "Status: Planned (Phase 3 — not implemented)," stale relative to the tested code underneath. What a real entry point requires, confirmed by direct source reading rather than assumed: deriving `GherkinStepNeed`s (text/step-type/captures) from real `.feature` text in the untracked per-run workspace (nothing in the repo does this derivation today); materializing a real, on-disk, buildable Maven project with generated Java already written into it before CP3 can be called (CP3 itself writes nothing); reconciling that CP3/CP4 verdicts are whole-run batch results (`AssetGateOutcomes`), not per-asset; and wiring promotion's direct `test-suite-baseline/` writes + `git add` (never `git commit`) into run-state stage 15. This is architecture — a deliberate, reviewed build, per this ADR's own existing item 14(a)/(b) deferrals above — not something to improvise inside a live integration run; none was written for this run. The live SonarQube server this run verified as reachable/authenticated/submission-capable (item 17 above) is ready for CP3 to reach the moment a driver exists, independent of when the `customqa:*` profile gap (item 18) is separately closed. **Owner:** Layer 3 wiring (a future stage-15 CLI/API/run-state integration task, `automation_engineering`). **Trigger to resolve:** a dedicated, reviewed build task — not an integration run — that adds the missing orchestration and, per ADR-0036, wires stage 15 into `run_state.json`.

    **RESOLVED (2026-08-05, the stage-15 wiring build — the dedicated, reviewed build task this item's own trigger named.)** `automation_engineering/stage/runner.py` (`run_automation_engineering_stage`/`execute_automation_engineering_stage`) chains all six subsystems, mirroring stage 14's own integration shape exactly. Every gap this item named is closed, in the same order: `GherkinStepNeed` derivation from real `.feature` text is built (`automation_engineering/stage/gherkin_needs.py`, reusing the SAME Gherkin parser CP2/CP3 already use — `captures` stays `()` deliberately, ADR-0044 D4's own deferral, a documented reuse-rate cost, never a safety one); the per-run workspace is materialized via the SAME `feature_engineering.stage.workspace.materialize_workspace` stage 14 already uses (one workspace per RUN, not per stage) and generated Java is written into it at the exact path `automation_engineering.promotion.identity.resolve_candidate_identity` resolves, before CP3/CP4 are ever called; CP3/CP4's own whole-run batch verdicts are threaded into `AssetGateOutcomes` per generated candidate exactly as that class's own docstring already specified (no per-asset granularity invented); promotion's `apply_promotion` writes + `stage_promoted_assets`' `git add` (never `git commit`) are wired into the stage's own final step. Registered as a LIVE stage (`requirement_intelligence/run_state/stages.py`: `automation_engineering` moved from `PLACEHOLDER_STAGE_IDS` into `LIVE_STAGE_IDS`, `governing_citation` corrected from "none yet" to "ADR-0044"). CLI-invocable via `analyze --with-automation-engineering` (`scripts/run_requirement_analysis.py`) — off by default, deliberately: this is a real, live-SonarQube-dependent stage (ADR-0044 D5), and gating it behind an explicit flag rather than the unconditional sequence avoids silently changing `analyze`'s own default behavior, mirroring exactly how stage 14 itself first shipped as a ready-to-call integration point before a separate later task hooked it in unconditionally. **One confirmed, not invented, scope boundary carried forward:** page-object/utility generation only fires as a NESTED side effect of step-definition generation via `page_object_request`/`utility_request` (`automation_engineering.generation.orchestrator`'s own optional parameters) — no subsystem derives "which specific page-object/utility method does this step need" from raw Gherkin text, the same class of gap `captures` derivation is, so this wiring never supplies them; CP4 therefore evaluates vacuously (no page objects to check) until a future task builds that per-step binding-request derivation. Proven end to end against a fixture feature set with stub matcher/generators/Sonar (`tests/unit/test_automation_engineering_stage.py`, 16 tests) — including the reuse-first loop closing THROUGH this real orchestration (a step definition promoted in run 1 is bound, not regenerated, in run 2), not merely the promotion package's own unit test. `make test`: 5348 → 5366 (+18: 16 new in `test_automation_engineering_stage.py`, 1 F3 goal-string proof, 1 stage-registration proof); `make lint`: clean; mypy (informational): 432 → 432, no rise. **Owner:** `automation_engineering/stage/`. **Trigger for the next task:** a genuine live end-to-end run (a reachable SonarQube server, real embeddings/LLM providers) — explicitly out of this build's own scope (verification here is entirely stub-based, no live call anywhere).

21. **New (2026-08-04), from the F4 discovery task — item 18's `customqa:*` gap is only PARTLY a "create the profile" task; the two rules are different kinds of things.** Full evidence and the per-rule decision surface are recorded as additive notes directly on the two ADRs that specify the profile, not duplicated here: ADR-0037 Recommendation 3 ("Discovery note, additive, 2026-08-04") and ADR-0044 D5 ("Discovery addendum, additive, 2026-08-04"). Summary: `customqa:long-method` is cheaply real — it IS the built-in `java:S138` ("Methods should not have too many lines," a configurable `max` parameter, verified via this integration's live server) — a profile activating it at `max=40` is a normal, versionable SonarQube profile export, exactly as ADR-0037/ADR-0044 already describe. `customqa:direct-webdriver-action` is NOT one existing rule and cannot be enabled the same way: no Selenium/architecture/layering rule exists in this server's Java catalog, and no custom-analyzer-plugin extension point was found to exist on it at all — the only genuinely native path found is composing a TEMPLATE rule (`java:S2253`/`S3688`, "disallowed methods/classes," global-only) with a separate project setting (`sonar.issue.ignore.multicriteria`) to exclude the ban for `com/automation/pages/**` — real, but a file-path approximation of an architectural constraint, not an exact expression of it, and it lives in project settings rather than quality-profile content. **The honest state: `customqa:*` is partly-aspirational, not fully.** One rule is cheap and real; the other has three legitimate paths (native rule-plus-exclusion composition, a custom Sonar plugin — heavyweight, unconfirmed as even supported here, or a static Python check inside Layer 3 itself, the same posture ADR-0044 D6/CP4 already uses for locator health) and choosing among them is a DESIGN decision, not a config task — explicitly not made by this discovery. No profile was created and no check was written; this item is discovery only. **Owner:** ADR-0037 (profile-artifact existence) / ADR-0044 D5 (which of the three paths `direct-webdriver-action` takes) jointly. **Trigger to resolve:** a future task decides `direct-webdriver-action`'s path (composed-native / custom-plugin / static-Python) and, together with activating `S138` at `max=40`, authors the actual `customqa:*` profile artifact item 18 already tracks as missing.

    **DECIDED (2026-08-04, the F4 revision — Path A, documentation-only, nothing built yet).** `direct-webdriver-action`'s path is chosen: a static Python check in Layer 3, not Sonar in any form (not the rule-plus-exclusion composition, not a custom plugin) — full rationale in ADR-0044 D5's own "Decision note" (this same date), with ADR-0037 Recommendation 3's "Companion note" (this same date) recording the consequence that the eventual `customqa:*` Sonar profile artifact will only ever contain the Sonar-expressible subset (`long-method`). CP3's own gate is now specified as a composite: the Sonar quality gate AND Layer 3's static architectural checks, both required to pass — correct partitioning of one verification obligation across the two mechanisms actually capable of each half, not a downgrade of either.

    **Two tracked, not-yet-built build tasks, both required before Layer 3's own live wiring (item 20) makes a meaningful CP3 run:**
    1. **Create the `customqa:*` Sonar profile** — a real, exportable SonarQube quality profile activating `java:S138` at `max=40` (plus whatever else the profile should carry), versioned in the tracked baseline per ADR-0037 Recommendation 3, and imported into whatever server CP3 targets (closes item 18's own original gap, for the Sonar-expressible subset).
    2. **Build the `direct-webdriver-action` static check in Layer 3** — inspects a generated step-definition class's own package/imports directly for a forbidden `org.openqa.selenium.WebDriver` reference, mirroring D6/CP4's "static, no SUT dependency" posture; wired into CP3 as the second, non-Sonar half of its composite gate.

    Sequencing, stated explicitly: until both land, Layer 3's stage-15 wiring (item 20) would produce a CP3 verdict that is honestly INCOMPLETE by this ADR's own now-corrected story — Sonar alone (against a profile that doesn't exist yet, or against `Sonar way`) was already known-wrong (item 18); a CP3 that ran Sonar without the static check would ALSO be incomplete, silently missing the architectural half entirely. Neither build task is started by this revision.

    **Build task (1) of 2 DONE (2026-08-04, artifact created and versioned; live import/assignment is a documented admin handoff, not a code gap).** `test-suite-baseline/sonar/customqa-profile.xml` is the real, versioned artifact — a full backup of the live server's own built-in `Sonar way` Java profile (`GET /api/qualityprofiles/backup`, not hand-typed: 549 rules, byte-verified round-trip through `xml.etree.ElementTree`), plus one addition, `java:S138` activated at `max=40` (its real, live-confirmed shape: native, non-template, default `max=75`, `MAINTAINABILITY`/`MEDIUM` impact). It does **not** claim `direct-webdriver-action` — task 2's own, separate obligation. `automation_engineering/cp3/sonar/live_adapter.py` now documents the expected profile name (`CUSTOMQA_PROFILE_NAME = "customqa"`) and, in its own module docstring, why no code change can select it: confirmed directly against the live server's own settings catalog that `sonar.profile` is not a recognized analysis parameter on this SonarQube version — profile-to-project assignment is exclusively a server-side administrative action (`POST /api/qualityprofiles/add_project`), never a scanner argument. **Import and assignment did not happen in this build**, and correctly did not: the pipeline's own `SONAR_TOKEN` carries no global permissions (`create`/`activate_rule`/`add_project` all returned "Insufficient privileges," verified directly, not assumed) — least privilege working as intended, not a bug to route around. `test-suite-baseline/sonar/README.md` is the exact, runnable one-time admin procedure (import, assign, and the scan-based proof — a >40-line method flagged by `java:S138`, a <40-line method clean) for whoever holds an admin token to execute; no scan verdict was faked in its place. **Owner of the remaining step:** a human with SonarQube admin rights, out-of-band, per the README. **Trigger:** that admin procedure is run once, against whatever server CP3 targets in a given environment — after which `test-suite-baseline/sonar/README.md`'s own proof section confirms it took.

    **Correction (2026-08-05, the Sonar-config discovery task — this sub-bullet's own claim is now stale).** The admin procedure directly above **did subsequently run**, later this same date (2026-08-04, per `test-suite-baseline/sonar/README.md`'s own "live proof" section): import (`ruleSuccesses:550`), project assignment (confirmed via `qualityprofiles/search`), and a real scan proving the mechanism (a 47-line method flagged under a temporarily-created `src/main` tree, a short method left clean) all happened, with an admin token, never the pipeline's own `SONAR_TOKEN`. So "import and assignment did not happen" is no longer the honest state of the WORLD — it describes only this one build's own scope, correctly, at the moment it was written. What supersedes it is not that claim being wrong, but a separate, larger finding the live proof itself surfaced: `java:S138` is permanently `scope:"MAIN"` and this platform's generated code is `src/test/java`-resident by design (ADR-0037 Path A) — the rule can never evaluate real generated code regardless of correct import/assignment. `long-method`'s Sonar path is therefore now SUPERSEDED by a static Layer 3 check (`automation_engineering.cp3.architecture.evaluate_long_method`), mirroring `direct-webdriver-action`'s own mechanism — see item 22 below and ADR-0044 D5's third revision note (2026-08-05) for the full account. The admin procedure's own success is not diminished by this — it is what produced the proof that made the supersession possible to state with confidence rather than guess at.

    **Build task (2) of 2 DONE (2026-08-04, fully live — no admin handoff needed for this half).** `automation_engineering/cp3/architecture.py` implements `customqa:direct-webdriver-action` as a sixth, hard CP3 criterion (`CRITERION_DIRECT_WEBDRIVER_ACTION`, `CP3_CRITERIA` now six entries): a static, `javalang`-based check that inspects each generated class's own PARSED `package` declaration (never a caller-supplied label, never a file-path convention) to decide whether it is a step-definition class in scope for the ban at all. Detects both proscriptions the rule names: an `import org.openqa.selenium.WebDriver` statement, and a direct method call on a WebDriver-typed field/parameter/local variable (matched by the invocation's own qualifier against every WebDriver-typed name declared in the class). Wired into `evaluate_cp3` (`automation_engineering/cp3/gate.py`) as a new `generated_classes` parameter (default `()`, passing vacuously — the same convention CP4's own empty `page_objects` input already establishes), composed alongside coverage and the Sonar gate; CP3's `overall_verdict` is `PASS` iff all six criteria pass, proven both directions (a clean Sonar scan does not rescue a direct-webdriver-action failure, and vice versa). **The class-role proof, the whole reason this is a static Layer 3 check and not a Sonar path-glob:** the identical WebDriver import-plus-direct-call, fed as two otherwise-identical classes in one evaluation, fails when the class's own package is `com.automation.steps` and is silently, correctly PASSED when it is `com.automation.pages` — proven directly (`tests/unit/test_automation_engineering_cp3_architecture.py`), not asserted. No network call, no subprocess, no browser, no Sonar — pure static analysis, mirroring ADR-0044 D6/CP4's own posture exactly. ~~**`customqa:*` verification is now COMPLETE, per the ADR-0044 D5 revision's own composite framing:** `long-method` via the Sonar profile (build task 1, artifact ready, live import pending a documented admin action) and `direct-webdriver-action` via this static check (build task 2, fully live today, no pending step).~~ **Superseded (2026-08-05, item 22 below): `long-method`'s Sonar path was subsequently found permanently unreachable (`java:S138` is `scope:MAIN`; this platform's generated code is `src/test/java`-resident, ADR-0037 Path A) and moved to a static check too — `customqa:*` verification is STILL complete, now by a fully-static composition of BOTH rules, neither Sonar-gated.** `make test`: 5328 → 5338 (+10: 8 in the new `test_automation_engineering_cp3_architecture.py`, 2 new composite-gate proofs in `test_automation_engineering_cp3_gate.py`); `make lint`: clean; mypy (informational): 432 → 432, no rise (`git stash -u` baseline re-verified directly before and after). **Owner:** `automation_engineering/cp3/architecture.py`. **Trigger for any further change:** a future, additional caller-class-role rule Sonar also cannot express would extend this same module, not spawn a parallel mechanism.

22. **New (2026-08-05), from the Sonar-config discovery task — found and resolved together: `java:S138`'s own `scope:MAIN` permanently defeats `customqa:long-method`'s Sonar verification, and it is moved to a static Layer 3 check.** Item 21's own live proof (`test-suite-baseline/sonar/README.md`) established the profile/rule mechanism works — a real 47-line method flagged, a short one clean — under a temporarily-created `src/main` tree. This task investigated whether that mechanism reaches this platform's REAL generated code, and found it structurally cannot: `java:S138`'s own rule metadata is permanently `"scope":"MAIN"` (confirmed live against the server, and already recorded in the README's own then-open "Open gap" section) — a rule-catalog property no quality-profile activation can override — while every class Layer 3 generates or promotes (step definitions, page objects, utilities) is deliberately placed under `src/test/java` (ADR-0037 Path A, `automation_engineering/catalog/scanner.py`'s own `JAVA_SOURCE_SUBPATH`). A `MAIN`-scope rule never evaluates `testSourceDirectory` content, on this or any project. **The one Sonar-side workaround investigated — repointing `sonar.sources`/`sonar.tests` so the generated-code tree is scanned as `MAIN`-scope — was rejected**, not merely set aside: it does not selectively re-scope `S138` alone, it activates every other `MAIN`-scope rule already in the `customqa` profile (the full `Sonar way` baseline it extends) against that tree at once, roughly 408 rules never reviewed for applicability to generated test-support code — a correctness hazard wearing a config tweak's clothes, not a targeted fix. **Resolution, found and closed in the same task:** `customqa:long-method` is verified instead by `automation_engineering.cp3.architecture.evaluate_long_method`, a static, `javalang`-based check mirroring `customqa:direct-webdriver-action`'s own mechanism exactly (same module, same static/no-SUT/no-Sonar/no-network posture) — measuring a method's line span (declaration line through closing-brace line, via `automation_engineering.catalog.java_source.declaration_line_span`, the same start/end anchors the catalog's own content-hash extraction already uses) against the rule's own 40-line threshold, applied to EVERY generated class (no class-role scope, unlike `direct-webdriver-action`). Wired into `evaluate_cp3` (`automation_engineering/cp3/gate.py`) as a seventh CP3 criterion (`CRITERION_LONG_METHOD`, `CP3_CRITERIA` now seven entries). **`customqa:*` verification is now FULLY STATIC** — both rules live against real parsed generated code, neither depends on Sonar or any live infrastructure; CP3's Sonar criterion remains a hard gate, now verifying generic Java quality only (the `Sonar way` baseline the `customqa` profile extends). The `customqa-profile.xml` artifact and its live import/assignment (item 21's own build task 1) are not wasted — they are what produced the proof that exposed this gap, and the profile continues to serve CP3's generic-quality gate; its own `S138` activation is left in place, superseded but harmless (this project has no tracked `src/main` tree for it to ever evaluate). Full rationale: ADR-0044 D5's third revision note (2026-08-05) and ADR-0037 Recommendation 3's second companion note (this same date); the operational record: `test-suite-baseline/sonar/README.md`'s own "Gap CLOSED" section. `make test`: 5338 → 5348 (+10: 8 new in `test_automation_engineering_cp3_architecture.py` — an at-threshold clean method, a one-over-threshold flag with the exact message text, all three generated-asset kinds flagged alike, a short-method-alongside-a-long-one discriminating proof, empty/unparsable/determinism proofs — plus 4 new composite-gate proofs in `test_automation_engineering_cp3_gate.py`); `make lint`: clean; mypy (informational): 432 → 432, no rise (`git stash -u` baseline re-verified directly before and after). **Owner:** `automation_engineering/cp3/architecture.py` (both static checks now live there). **Trigger for any further change:** a future rule Sonar also cannot express (by scope or by kind) would extend this same module, not spawn a parallel mechanism — the same trigger item 21's build task 2 already stated for `direct-webdriver-action`, now shared by both.

23. **New (2026-08-05), from a live Layer 1-3 end-to-end run against the free tier — six findings that block or destabilize a completing run, all traced to the SAME root cause the batching investigation surfaced first.** The run died on the first embedding call against a near-empty catalog. Investigation, reported honestly rather than assumed: `automation_engineering.reuse.live_matcher.LiveSemanticMatcher.match()` already batches ONE call per invocation (the need's text plus every catalog asset's text together, `embed_content(contents=[...])`) — ADR-0044 D3's own batching rationale, realized at that granularity. The gap was one level up: `match()` itself was called ONCE PER NEED, in a loop (`automation_engineering.generation.orchestrator.generate_step_definitions` → `orchestrate_step_definition` → `decide_reuse` → `matcher.match(need, catalog)`, exactly once per need by the engine's own documented contract) — so a run with N unique step-needs made N separate `embed_content` calls, each REDUNDANTLY re-embedding the same unchanged catalog every time. Call-count math: a 20-need run against a catalog of M assets made 20 calls of `(1+M)` texts each, not 1; against the run's own measured free-tier limit (`embed_content_free_tier_requests`, 100/minute), 20 calls is close to the edge before any other embedding traffic, and a larger corpus crosses it outright. Batching WAS the primary lever — confirmed, not assumed, by fixing it and proving the call count collapses (below). ADR-0044 D3 records this as "a lean toward embeddings... not a lock, the exact mechanism deferred to implementation" — the fix below fulfills that lean's own stated rationale more completely than the pre-fix code did; no Accepted ADR conflicts with it.

    **RESOLVED (2026-08-05, the free-tier survivability build — all six fixed, per-item isolation applied consistently across the three seams that lacked it: embeddings, stage 15, and the connector layer).**

    **FIX 1 — whole-run embedding batching.** `SemanticMatcher` (the Protocol, `automation_engineering/reuse/matcher.py`) gained a second method, `prime(needs, catalog)` — a whole-run warm-up, optional in effect (a no-op is a valid implementation; `StubSemanticMatcher.prime()` does nothing). `LiveSemanticMatcher` (`.reuse/live_matcher.py`) implements it for real: embeds every need's text plus every catalog asset's text together, once, deduplicated, caching the result by exact text; every subsequent `match()` call in the same run then finds its own texts already cached and makes ZERO further `embed(...)` calls (a cache miss still falls back to embedding on demand — `match()`'s behavior for a caller that never primes, e.g. every pre-existing test, is byte-for-byte unchanged). `automation_engineering/stage/runner.py::run_automation_engineering_stage` calls `matcher.prime(...)` once, before its own per-need loop. No per-call size cap is documented for the actual code path this provider uses (the Gemini Developer API via `google-genai`'s non-Vertex `embed_content`, confirmed directly against the installed SDK's own source — the "one content at a time" restriction some documentation describes is Vertex-AI-specific and is never reached here); none was invented. Proven: `tests/unit/test_automation_engineering_reuse_live_matcher.py::test_prime_then_match_for_several_needs_makes_exactly_one_call` — 3 needs against a 2-asset catalog, `prime()` then 3 `match()` calls, exactly 1 `embed()` call total (was 3 before this fix, and would have been N for N needs).

    **FIX 2 — stage 15 transport isolation (the F1/F2 pattern, un-inherited by Layer 3 until now).** A new package-level exception, `automation_engineering.errors.TransportFailureError`, mirrors `feature_engineering.generation.errors.TransportFailureError` — placed at the package root (not under `.generation/`, Layer 2's own location) because Layer 3 has TWO boundaries needing it, MATCH (embeddings) and GENERATE (four independent LLM generators), neither a natural subpackage of the other. `EmbeddingCallError` (`.reuse/embeddings.py`) and all four `LiveGenerationError` classes (`live_step_definition_generator.py`/`live_page_object_generator.py`/`live_utility_generator.py`/`live_test_data_generator.py`) now subclass it — a re-parenting, not a behavior change to any failure mode. `run_automation_engineering_stage` no longer calls the batch `generate_step_definitions`/`generate_test_data_classes` helpers; it loops per need (`orchestrate_step_definition`) and per test-data specification (`generate_test_data_class`) directly, catching `TransportFailureError` at each iteration and recording it as an escalated `AssetRecord` (`escalation_check="transport"`, distinct from the reuse engine's own three deterministic `EscalationCheck` values — `confidence`/`content_hash`/`signature_fit`/`method_fit` — which this fix never touches, preserving ADR-0044 D4's closed three-check vocabulary) — the loop CONTINUES with the rest. The shared `matcher.prime(...)` warm-up call is ALSO wrapped (`except TransportFailureError: pass`): priming is a call-count OPTIMIZATION (FIX 1), not a correctness requirement, and letting a failed BATCHED priming call propagate would silently defeat FIX 2's own per-need isolation for every need at once — swallowed, `match()` falls back to on-demand embedding, where a persistent failure is still caught and escalated one need at a time. Stage status stays `SUCCEEDED` (never `FAILED`) when only transport failures occurred — no new `StageStatus` value invented, mirroring Layer 2's own convention exactly (`SUCCEEDED` plus non-empty `escalated_records` is the whole signal). A genuine NO_MATCH still generates normally in the same run as a transport-escalated need (proven side by side). Proven: `tests/unit/test_automation_engineering_stage.py::TestTransportFailureIsolation` (7 tests — a generation-call failure escalates one need while the others still generate; an embedding-call failure does the same; a transport escalation is distinguishable from a genuine `confidence` escalation in the same run; a test-data specification's own transport failure escalates that one spec while others still generate; a failure during the shared `prime()` call is swallowed and every need still generates via on-demand fallback; the run-state proof — `execute_automation_engineering_stage` records `succeeded` with `has_escalations=True`, never `failed`).

    **FIX 3 — pacing and bounded retry-with-backoff on embedding calls, at the embedding-call boundary only.** Two mechanisms inside `GeminiEmbeddingProvider._execute`/`._pace` (`.reuse/embeddings.py`), never woven into `.live_matcher` or the reuse engine: (a) proactive pacing — before every call, blocks just long enough to keep the provider's own trailing-60-second call count under `max_calls_per_minute` (default 90, a margin under the free tier's measured 100/minute); (b) reactive retry-with-backoff — a retryable failure (429/`RESOURCE_EXHAUSTED`, or 5xx/`UNAVAILABLE`, detected via `google.genai.errors.APIError`'s own `code`/`status` attributes) is retried up to `max_retries` times (default 4), honoring the SDK error's own `retryDelay` when parseable from `.details` (Google's `RetryInfo` proto), falling back to exponential backoff (`2.0 * 2**attempt`, capped at 60s) otherwise. A non-retryable failure (anything else) is wrapped and raised immediately, unchanged. Retry exhaustion still raises `EmbeddingCallError` — bounded, not infinite; FIX 2's per-need isolation is the backstop when retry alone isn't enough, exactly as intended. This is the transport-retry-with-backoff item 16(a)'s own resolution note tracked as "a possible, deliberate follow-up, not smuggled into that fix" — now built, deliberately, here; no Accepted ADR addresses retry/backoff (a repo-wide search of `docs/adr/*.md` for "retry"/"backoff" returns nothing), so there was nothing to conflict with. `sleep`/`clock` are constructor-injectable so tests prove the schedule with zero real delay. Proven: `tests/unit/test_automation_engineering_reuse_embeddings.py` — one 429 then success (backs off once, succeeds on retry 2); the SDK's own `retryDelay` honored over the exponential fallback; a persistent 429 exhausts `max_retries` and raises (`4` retries + first attempt = `5` calls, `4` sleeps); a non-retryable failure never sleeps or retries; pacing sleeps exactly the remaining window time once `max_calls_per_minute` is reached.

    **FIX 4 — `--resume` extended to `automation_engineering` (the F2 gap, for the stage wired in after F2 landed).** `scripts/run_requirement_analysis.py::handle_analyze`'s "already complete" check now also inspects the `automation_engineering` stage's own status, gated on THIS invocation's `--with-automation-engineering` flag (stage 15 is off by default and only ever attempted when that flag is set) — mirroring F2's own "not attempted, legitimately, must not block complete" / "attempted and not `SUCCEEDED`/`SKIPPED`, must resume" split exactly, one flag-check further than F2's own `feature_engineering` gate needed. Proven end-to-end through the real CLI (`tests/unit/test_run_requirement_analysis.py::test_resume_detects_a_failed_automation_engineering_stage_and_resumes_it`): run 1 fails stage 15 outright (a fake matcher's `prime()` raises a genuine, non-transport exception — no live call, no real baseline touched); `--resume --with-automation-engineering` correctly reports NOT complete and re-attempts it; run 2 (a safe fake matcher/test-data-generator that transport-escalate everything, and a stub Sonar adapter — nothing is ever generated or promoted, so the REAL tracked `test-suite-baseline/` is never written to) completes with `succeeded`, proving genuine re-attempt, not a skip.

    **FIX 5 — `ConnectorRegistry` per-source isolation.** `execute_all()` (`requirement_intelligence/registry/connector_registry.py`) now catches `ConnectorError` (the configuration/connection/fetch family — an infra condition local to one source) per source, records it as a new `SourceExecutionFailure(source_id, source_name, error)` in the registry's own `failed_sources` property, and CONTINUES with the remaining enabled sources — a down/unreachable connector (the run's own "ZAP unreachable" finding) no longer aborts the whole run before Layer 1 produces anything. `RegistryValidationError` (a mapper-instantiation/metadata bug) is deliberately NOT isolated — that class of failure is a genuine setup defect, not a source being temporarily down, and this registry's own `__init__`-time validation exists to catch it loudly; isolating it would hide a bug. The CLI (`scripts/run_requirement_analysis.py::run_engineering_pipeline`) reports each source's REAL outcome (a per-source `console.error` line plus a "N of M source(s) unavailable, proceeding degraded" note) instead of a blanket `console.ok` that would otherwise misreport a source `execute_all()` had just skipped. Downstream, a fully-absent source needs no new handling: the existing coverage-guaranteed `DefaultOrchestrationPolicy` (ADR-0015, CAP-076D) already reports a zero-evidence domain transparently rather than silently dropping it — this fix only ensures Layer 1 actually reaches that policy with whatever evidence the up sources produced, instead of reaching nothing at all. Proven: `requirement_intelligence/tests/unit/test_registry.py` (one source pointed at a nonexistent file — `execute_all()` does not raise, the other enabled sources' artifacts are present, `failed_sources` names exactly the broken one; a clean run reports zero failures; `failed_sources` reflects only the MOST RECENT `execute_all()` call, not accumulated history) and `tests/unit/test_run_requirement_analysis.py` (the CLI's own per-source reporting, both the degraded and the all-clean case).

    **FIX 6 — embedding default model.** `GeminiEmbeddingProvider`'s default (`.reuse/embeddings.py`) changed from `text-embedding-004` (404s against the live run's real API key) to `gemini-embedding-001` (what the key actually serves) — `EMBEDDING_MODEL` still overrides either way. No ADR pins the model name; this was purely an implementation default.

    **Combined effect (by design/math — the live re-run proof is the next task's, this is a no-live-call fix task).** A run's embedding call count no longer scales with need count (FIX 1: ~1-2 calls for a run's whole need set, not N); the calls that do happen stay paced under the measured 100/minute limit and survive a transient 429 via bounded retry (FIX 3); any call that still fails after retry — or any generation call, which carries no retry — escalates only the one need/specification it belongs to, never the whole stage (FIX 2); a resumed run correctly re-attempts a genuinely failed stage 15 instead of reporting false completion (FIX 4); a down evidence source degrades the run instead of aborting it before Layer 1 produces anything (FIX 5); and the default model matches what the live key actually serves, so a from-scratch run does not 404 immediately (FIX 6). Together: a live run's failure modes shrink from "one rate limit anywhere kills the whole pipeline" to "one rate limit, after a bounded retry, escalates one item and the run completes."

    Verified: `make test`: 5366 → 5387 (+21: 4 in `test_automation_engineering_reuse_live_matcher.py` — FIX 1's `prime()`/cache proofs; 5 in `test_automation_engineering_reuse_embeddings.py` — FIX 3's retry/backoff/pacing proofs, plus the FIX 6 default-model rename; 6 in `test_automation_engineering_stage.py` — FIX 2's `TestTransportFailureIsolation`, 5 tests, plus the `prime()`-failure-swallowed proof; 3 in `test_run_requirement_analysis.py` — FIX 4's resume proof plus FIX 5's CLI-reporting proofs, 2 tests; 3 in `requirement_intelligence/tests/unit/test_registry.py` — FIX 5's isolation proofs); `make lint`: clean; mypy (informational): 432 → 432, no rise (`git stash -u` baseline re-verified directly before and after this build, not assumed from memory). Existing Layer 2 F1/F2, stage 15, reuse, catalog, and CP3/CP4 tests unchanged and pass. **Owner:** `automation_engineering/reuse/`, `automation_engineering/stage/runner.py`, `automation_engineering/errors.py` (new), `automation_engineering/generation/live_*_generator.py`, `requirement_intelligence/registry/connector_registry.py`, `scripts/run_requirement_analysis.py`. **Trigger for the next task:** a real live re-run against the free tier, to prove by measurement (not math) that the combined effect above actually holds — explicitly deferred, per this task's own scope boundary (no embedding cache/vector store/Neo4j; that is a separate, later, evidence-driven decision made only after this re-run shows whether call volume is still a problem).

24. **New (2026-08-05), from the live re-run item 23 itself deferred — two findings, one resolved here, one tracked.** The re-run this platform's real `GOOGLE_API_KEY` (`output/executions/run-20260805T105026014856Z-987575c0/automation_engineering_report.md`, 69 needs processed) surfaced two things, neither a regression of item 23's own fixes.

    **Finding 1 — `NO_MATCH`/generate was bootstrap-only, RESOLVED.** All 49 real step-definition needs (SauceDemo login/cart/checkout/logout Gherkin steps) matched against the tracked baseline's real 2-asset catalog (`SmokeSteps`'s own two steps, unrelated to any of the 49) scored in `[0.5707, 0.6688]` — every one escalated on confidence (ADR-0044 D4(a)), zero generated, zero bound. Root cause, confirmed by direct source reading: `LiveSemanticMatcher.match` (`automation_engineering/reuse/live_matcher.py`) returns one `MatchCandidate` per catalog asset UNCONDITIONALLY (`if not assets: return ()` is its only empty-result path); `decide_reuse` (`automation_engineering/reuse/engine.py`) only ever produced `NoMatch` when `matcher.match` returned `()` outright. Once a catalog has ever been populated (i.e. post-bootstrap, the normal case for every run after the first), `NoMatch`/generate was therefore UNREACHABLE — every need, including a genuinely novel one matching nothing, escalated instead of generating, contradicting D3's own "Reuse First, Generate Later" discipline (generation is for "an asset with no acceptable reuse match," not merely "an asset without an already-trusted one"). **Resolved**: `decide_reuse` gained a third tier, `DEFAULT_GENERATE_FLOOR = 0.70`, sitting strictly below ADR-0044 D4(a)'s own `DEFAULT_CONFIDENCE_THRESHOLD = 0.75` — `best.confidence < generate_floor` now returns `NoMatch` (generate), `generate_floor <= best.confidence < confidence_threshold` still escalates exactly as D4(a) always did, `best.confidence >= confidence_threshold` still runs D4's full three-check safety (content-hash, structural fit), entirely UNCHANGED. **The floor is calibrated from real data, not guessed**: the live run's own 49-need band (`[0.5707, 0.6688]`) plus a fresh live probe against two more deliberately-unrelated needs (`[0.5196, 0.5696]`, same catalog, same embedding model) establish the real "nothing relevant" ceiling at 0.6688; the SAME catalog's own exact-text match scored 0.9843 and a full paraphrase scored 0.8980 — the real "genuinely relevant" floor. These two observed bands do not overlap (a 0.23-point gap), so a single global floor cleanly separates them without needing a relative-gap or normalized criterion; 0.70 sits at the midpoint between the observed unrelated ceiling and D4(a)'s own existing threshold, with margin on both sides. Proven directly: a need scoring below the floor against a NON-empty catalog now reaches `NoMatch` (`tests/unit/test_automation_engineering_reuse_engine.py::test_below_floor_similarity_is_no_match_not_escalation_with_a_non_empty_catalog`), the floor/threshold boundary is contiguous at both edges, and the floor is independently configurable, mirroring `confidence_threshold`'s own existing configurability. D4's three-check safety is untouched — the new tier only affects candidates that were never plausible in the first place, so it cannot mask a should-have-bound or should-have-escalated case (the danger direction, a wrong bind, is still gated by the unchanged three checks). Six pre-existing unit tests (`test_automation_engineering_reuse_engine.py`, `test_automation_engineering_stage.py`, and the three generation-orchestrator test modules) encoded the OLD two-tier assumption directly — a stub confidence of `0.1`/`0.5` used to represent "any sub-threshold confidence escalates" — and were updated to a confidence inside the new escalate band (`0.72`) to preserve each test's own original intent (proving escalation-path behavior) rather than being incidentally invalidated by the calibration; each such edit is called out inline at its own site. Verified: `make test`: 5387 → 5390 (+3, all in `test_automation_engineering_reuse_engine.py`); `make lint`: clean; mypy (informational): 432 → 432, no rise. See ADR-0044 D3/D4's own additive note (`docs/adr/0044-layer-3-automation-engineering-architecture-freeze.md`) for the full account. **Owner:** `automation_engineering/reuse/engine.py`.

    **Finding 2 — generation-call transport has no retry, RESOLVED.** `GeminiEmbeddingProvider` (item 23's FIX 3) gained pacing and bounded retry-with-backoff for embedding calls specifically; the actual GENERATION calls (`LiveFeatureContentGenerator`/`LiveFeatureRemediator`'s own single-call, no-retry discipline, and Layer 3's own `live_*_generator.py` peers) still made exactly one call and surfaced whatever came back — a `429 RESOURCE_EXHAUSTED` on a generation call still failed that one need/specification outright (isolated to it alone, per item 23 FIX 2's transport-isolation discipline, but not retried). **Resolved**: `requirement_intelligence/llm/providers/gemini_provider.py::GeminiProvider._execute` — the ONE concrete `generate_content` call site every generation caller in the platform shares (Layer 1/2's own `LiveFeatureContentGenerator`/`LiveFeatureRemediator` AND Layer 3's four live generators, all via constructor-injected `LLMProvider`) — gained the identical pacing/bounded-retry-with-backoff shape as FIX 3's embedding boundary: `DEFAULT_MAX_CALLS_PER_MINUTE = 12` (a defensive margin under the free tier's own measured 15/minute `generate_content` quota, proportionally tighter than FIX 3's 90-under-100 since 15/minute leaves far less headroom), `DEFAULT_MAX_RETRIES = 4`, honoring the SDK error's own `retryDelay` (the live run's own observed 0.9-1.5s delays) before falling back to exponential backoff. **Mirrored, not shared, with FIX 3** — deliberately: the two boundaries differ in SDK call (`generate_content` vs. `embed_content`), exception taxonomy to preserve (`ProviderGenerationError`, this provider's own pre-existing contract, vs. `EmbeddingCallError`), real quota (15/minute vs. ~100/minute), and subsystem (`requirement_intelligence.llm.providers` vs. `automation_engineering.reuse`) with no existing cross-import precedent between them; a shared helper for exactly two call sites would be the premature abstraction, not the fix (full rationale in `gemini_provider.py`'s own module docstring). **Zero changes needed to any of the six call sites** (`LiveFeatureContentGenerator`, `LiveFeatureRemediator`, and Layer 3's four `live_*_generator.py` modules): each already catches this provider's exception broadly and re-wraps it in its own boundary's transport-failure type, and retry exhaustion still raises `ProviderGenerationError`, unchanged in TYPE, so FIX 2's per-need/per-specification escalation remains the unchanged backstop when the bounded retry is not enough. Proven (stubbed, no live call): a transient 429 recovers and the asset generates; a persistent 429 exhausts the bound and raises `ProviderGenerationError` (FIX 2 still escalates); `retryDelay` is honored over the exponential fallback; a non-retryable failure never retries; pacing blocks once the per-minute limit would be exceeded. Verified: `make test`: 5390 → 5395 (+5, all in `requirement_intelligence/tests/unit/test_gemini_provider.py`); `make lint`: clean; mypy (informational): 432 → 432, no rise. **Owner:** `requirement_intelligence/llm/providers/gemini_provider.py`.

25. **New (2026-08-05), the milestone — the reuse loop closed LIVE, end to end, for the first time: generate → CP3/CP4 → promote → reuse, across two real runs (`run-20260805T161514117211Z-52e34458` then `run-20260805T164229854924Z-cb166b6b`), real matcher, real Sonar, real promotion write, real second-run bind.** Item 24's two fixes (the generate-floor tier, the generation-boundary retry) made this reachable; this run is the confirmation, not a build — no code changed here.

    **RUN 1** (`...52e34458`): 80 needs processed (60 step-definition, 20 test-data), **80 generated, 0 bound, 0 escalated**. Every step-definition need scored below `DEFAULT_GENERATE_FLOOR` (e.g. `"the user is on the login page"` at 0.6615, the same real score item 24's own investigation already recorded for this exact text against this exact 2-asset catalog) — the three-tier fix's below-floor→generate path, live. **CP3: pass** (all seven criteria, including the live Sonar quality gate and both static `customqa` checks — zero violations on real LLM-generated Java). **CP4: pass.** **Promoted: 60** (test-data is never promotion-eligible, ADR-0044 D7, unaffected). `git status` showed 33 new files staged under `test-suite-baseline/src/test/java/com/automation/steps/` — the platform's first-ever legitimate promotion write — subsequently **committed** (`c05aa9f`, "Base Line Step Definitions"): the tracked baseline now genuinely carries 35 step-definitions, not 2.

    **RUN 2** (`...cb166b6b`): catalog reconciled to the new 35-asset baseline. **30 bound, 30 escalated, 0 newly generated** (the 20 "generated" this time are test-data specs again — run 1's promotion already covers this requirement domain). **Bind proven live with real, strong scores**, verified directly against the actual reconciled catalog: `"the user authenticates with valid credentials"` → 0.9690 → bound to `AuthenticationSteps` (the exact class run 1 created); `"the system should display an access denied message"` → 0.9880 → bound to `AccessDeniedSteps`; generator not called for either. `git status` after run 2 showed the SAME 33 files, nothing further staged — reuse without regeneration, confirmed. **The middle band fired too, live, unplanned, and for two genuinely distinct reasons**: (a) confidence near-miss — `"the user is on the login page"` (identical text to run 1's) now scores 0.7455 against the class the LLM generated for it, just under the 0.75 bind threshold, because the LLM's own generated Cucumber pattern paraphrases rather than reproduces the need text verbatim; (b) signature-fit mismatch — e.g. `"the inventory page should be displayed"` semantically matches an asset needing 1 capture while the need has 0, D4(c) catching a real near-miss on live data, not a contrived one. **All three tiers of the generate-floor fix, and the full generate→promote→reuse loop, are now proven on real data, not merely unit-tested.**

    **Where it only half-closed, honestly:** run 2 promoted **0** assets despite 30 clean binds, because CP3's own coverage criteria (`step_coverage`/`scenario_coverage`/`unmapped_steps`) count an escalated step as unmapped, and 30 real escalations therefore failed CP3 — see item 26 below for why that then blocked ALL of run 2's promotion, not just the escalated needs' own. This is not a defect in either of item 24's fixes — the reuse-decision logic worked correctly in both directions in both runs; it is a pre-existing, whole-run-granularity promotion property (ADR-0045 D2(a)), never previously observable because the loop had never closed enough to reach it.

    **No quota pressure was exercised.** Every escalation across both runs was `confidence` or `signature_fit` — `escalationCheck` was never `transport` in either run. Item 24's generation-boundary retry (Finding 2) is therefore still proven only by its own unit tests, not yet by a live 429 — see item 27.

    Full detail, verification against real code, and the two new open items this milestone surfaced: items 26 and 27 below. **Owner:** none — this item is a confirmation record, not an open action itself.

26. **New (2026-08-05), from item 25's own live run — promotion gates PER-RUN, not per-asset; a real clean bind/generate is blocked by an unrelated escalation in the same batch.** ~~Design decision recorded, not yet built~~ **RESOLVED (2026-08-06), design locked and built.** Verified directly against the real code, not inherited from the run report: `automation_engineering/promotion/gate.py::evaluate_promotion` calls `gates.first_failure()` on one `AssetGateOutcomes` instance shared by every candidate in the run (`automation_engineering/stage/runner.py`'s own `gates = AssetGateOutcomes(cp2_verdict=..., cp3_verdict=cp3_result.overall_verdict, cp4_verdict=cp4_result.overall_verdict)`, built once, threaded into every `promote_outcome(outcome, gates, tracked_catalog)` call in the batch) — `AssetGateOutcomes`'s own docstring already states this plainly ("`cp3_verdict`/`cp4_verdict` are supplied at the granularity CP3/CP4 are actually computed at TODAY: one verdict for the whole batch... neither gate is computed per individual asset in this platform yet"), so this is a KNOWN, DELIBERATE, already-documented property (ADR-0045 D2(a)), not a hidden defect — item 25's run is simply the first time it was ever actually exercised, because it is the first run to ever have BOTH a clean candidate AND an escalation in the same batch. Confirmed the mechanism that trips it: `automation_engineering/cp3/coverage.py::_is_mapped` counts only `GeneratedStepDefinition | BoundStepDefinition` as mapped — an `EscalatedStepNeed` counts as unmapped, so `step_coverage`/`scenario_coverage`/`unmapped_steps` (ADR-0044 D5's own always-meaningful gates) correctly fail whenever any step in the batch escalates. The two correct, independent behaviors compose into one un-designed-for outcome: 30 genuinely clean binds (and, in a future run, genuinely clean generates) are withheld from promotion because 30 OTHER needs in the same batch correctly escalated. **Decision recorded here: promotion should gate PER-ASSET, not per-run** — a clean candidate should promote regardless of what else in the same batch escalated; only CP3/CP4 criteria that are inherently whole-batch in nature (the Sonar scan, the coverage percentage as a REPORTED figure, ADR-0044 D5) remain batch-level, but the PROMOTION gate itself should not inherit that granularity wholesale for candidates that individually passed. This is a scoped design-and-build task, not a hotfix: it touches ADR-0044 D5's coverage-verdict semantics and ADR-0045 D2's own gate contract, and needs its own design pass (e.g. a per-asset CP3/CP4 record, not merely reusing the run's single `Cp3Result`/`Cp4Result.passed`, which `AssetGateOutcomes`'s own docstring already flags as the missing prerequisite: "A future Layer-3 stage runner may persist a genuinely per-asset CP3/CP4 record; building that manifest is out of this ADR's own scope"). **Owner:** `automation_engineering/promotion/gate.py`, `automation_engineering/cp3/gate.py` (per-asset record production), ADR-0044 D5 / ADR-0045 D2 (the amendment). **Trigger to resolve:** a dedicated design-and-build task, not folded into a future confirmation run.

**RESOLUTION (2026-08-06), the dedicated design-and-build task this item itself named.** The design decision recorded above ("promotion should gate PER-ASSET, not per-run") is now locked as additive notes on ADR-0044 D5 and ADR-0045 D2 (this same date) and built. The decomposition, honestly: three CP3 criteria (`direct_webdriver_action`, `long_method`, `duplicate_steps`) already carry real per-class/per-asset attribution in their own message text and are filtered per-candidate, no re-scan; the coverage family (`step_coverage`/`scenario_coverage`/`unmapped_steps`) is excluded from a per-candidate gate entirely — a candidate reaching the gate is by construction a `Generated*`/`Bound*` outcome with its own step already mapped, so this family can only ever fail because of a DIFFERENT, unresolved need, never this one; `sonar_quality_gate` does NOT decompose and stays whole-batch, honestly, because `SonarQualityGateResult` carries no file/class attribution at all — this is the one real constraint the design task was asked to be honest about, and it is. Built in `automation_engineering/promotion/cp3_decomposition.py` (`decompose_for_asset`), wired through `AssetGateOutcomes.first_failure` (now `class_name`/`asset_id`-scoped, `automation_engineering/promotion/models.py`) and `evaluate_promotion` (`automation_engineering/promotion/gate.py`); `automation_engineering/stage/runner.py` now threads the whole `Cp3Result` into `AssetGateOutcomes` (`cp3_result=cp3_result`) instead of collapsing it to `cp3_result.overall_verdict` first. Proven against the EXACT scenario this item reported: `tests/unit/test_automation_engineering_stage.py::TestPerAssetPromotionAcrossEscalations::test_clean_generates_promote_despite_an_unrelated_escalation_in_the_same_batch` (a mixed batch — clean generates plus one unrelated escalation — now promotes the clean ones; the whole run's own `Cp3Result.overall_verdict` still correctly reports FAIL, but that no longer blocks promotion of the resolved, clean assets) and `::test_a_per_class_static_check_failure_is_isolated_to_that_class` (one class's own static-check violation blocks only that class, not a different clean class in the same run); `tests/unit/test_automation_engineering_promotion_gate.py::TestPerAssetCp3Decomposition` (6 tests, unit-level: per-class/per-asset criteria isolate correctly, coverage-family failures from unrelated needs never block, whole-project Sonar still blocks everyone). ADR-0045 D2(b)'s non-duplicate check, D3's escalation-to-shared-review, and D5's stage-for-review mechanism are all UNCHANGED — this was a granularity change to D2(a)'s own evaluation, not a redesign of any other promotion rule. `make test`: 5395 → 5403 (+8, all new: 6 in the promotion-gate unit test, 2 in the stage-level integration test); `make lint`: clean; mypy (informational): 432 → 432, no rise.

27. **New (2026-08-05), from item 25's own live run — two baseline-static unit tests now fail, because the loop they didn't anticipate actually closed. Suite is RED; needs a near-term fix, separate from item 26's design task.** Verified directly: `tests/unit/test_automation_engineering_catalog.py::TestRealTrackedBaseline::test_smoke_steps_are_catalogued` and `tests/unit/test_automation_engineering_catalog_alignment.py::TestRealBaselineAlignment::test_smoke_steps_are_aligned` both hard-assert `len(catalog.step_definitions) == 2` after calling `reconcile()` against the REAL, LIVE `test-suite-baseline/` (not a fixture copy) — true when written (the tracked baseline had never been promoted into), false now that item 25's run legitimately grew it to 35 and committed that growth (`c05aa9f`). `make test`: **5393 passed, 2 failed** (was 5395/5395 clean immediately before this run). Not a regression in either of item 24's fixes, or in anything this milestone touched — the two tests were always coupled to the live tracked baseline's exact historical size, and the platform's own central thesis (reuse persists and grows the baseline across runs, ADR-0044 D3) guarantees that size will keep changing. **The same class of coupling** `tests/unit/test_feature_engineering_stage.py::test_tracked_baseline_is_never_written_to` already lives with (its own comment records exactly this tension: "the invariant this test actually promises is 'the STAGE causes no git-visible change,' not 'the tree has zero uncommitted diff for any reason,' which only ever coincided by chance") — that test survives by checking a DELTA (byte-identity/git-status unchanged across one call) rather than an absolute count, which is the shape these two tests should move to as well: assert against a STABLE FIXTURE COPY of the original 2-asset baseline (or an absolute-count-free invariant), not a hardcoded `== 2` against the live tracked directory promotion now mutates. **Owner:** `tests/unit/test_automation_engineering_catalog.py`, `tests/unit/test_automation_engineering_catalog_alignment.py`. **Trigger to resolve:** a small, near-term test-fix task — the suite is red until this lands, and every future promotion-writing run will only grow the gap further.

    **RESOLVED (2026-08-05, a small test-fix task — production code and the live tracked baseline both untouched).** Chose the specific-asset invariant over a fixture copy: both tests already look up `SmokeSteps`'s own two methods by name (`by_method["iOpenTheApplicationUnderTest"]`/`["thePageTitleIsNotEmpty"]`) rather than iterating positionally, so the ONLY line coupling them to an exact population size was the `len(...) == 2` assertion itself (`test_smoke_steps_are_catalogued`) and the blanket `for step in catalog.step_definitions:` loop (`test_smoke_steps_are_aligned`, which would otherwise now also assert "zero captures" against promoted steps that genuinely have some). `test_smoke_steps_are_catalogued`: `== 2` relaxed to `>= 2` ("SmokeSteps' own two, plus whatever else"); the existing by-method lookups are unchanged and still verify the real, exact pattern/step-type/class-name/parameters. `test_smoke_steps_are_aligned`: the blanket loop narrowed to iterate only `("iOpenTheApplicationUnderTest", "thePageTitleIsNotEmpty")` via the same `by_method` lookup, preserving every original assertion (captures/correlations/non-capture-parameters/is-aligned/mismatch-reason) scoped to the two known-captureless methods, not the whole (now-mixed) catalog. A fixture copy was considered and rejected: every OTHER test in `test_automation_engineering_catalog.py` already proves "the scanner/aligner works on a known input" via `tmp_path` + the file's own `_write_java` fixture helper — a fixture copy of `SmokeSteps` here would duplicate that coverage under a different name, while these two tests' own stated intent (their class docstrings, both still accurate) was always "does the REAL, live baseline's actual `SmokeSteps` get catalogued/aligned correctly," which the specific-asset form verifies more directly and with a smaller diff. Both class docstrings updated in place to state the corrected invariant and cross-reference `test_framework_and_runner_packages_are_excluded`/`TestJsonRoundTrip.test_signature_alignment_round_trips_through_json`, which already used the identical "known asset present, not exact population" shape and were never broken by this gap. Verified: `make test`: 5393 → **5395 passed, 0 failed** (fully green); `make lint`: clean; mypy (informational): 432 → 432, no rise; `git status --porcelain` touches only the two test files — no production code, no further live-baseline write. **Residual, noted, not fixed (out of this task's explicit scope):** `TestRealTrackedBaseline.test_smoke_page_is_catalogued_as_page_object` (same file) still hard-asserts `len(catalog.page_objects) == 1` against the live baseline — currently true (this run's promotion only ever produced step definitions) but the identical fragility class, latent until a future run promotes a page object.

28. **New (2026-08-06), CP5 (ADR-0046) given the live entry point it lacked — Layer 4's own stage 16, wrapping stage 15's per-asset promotion, mirroring the stage-15 wiring build (item 20) one layer up.** `suite_quality_governance/README.md` recorded CP5 as fully built (all four components, ADR-0046 D2-D5) but with "no CLI or HTTP entry point, no run-state stage wiring" — this task closed that gap by COMPOSING the already-built capstone (`suite_quality_governance.cp5.promotion_wrap.evaluate_promotion_wrap`), not by building any new CP5 logic. **Insertion point:** immediately after `automation_engineering.promotion.mechanism.stage_promoted_assets` (`git add`, never `git commit`) returns inside stage 15's own `run_automation_engineering_stage` — the exact wrap order ADR-0046 D4 locks. **Run-state choice, reported per this task's own governing rule ("where this prompt and an Accepted ADR disagree, the ADR wins"):** the task's own pre-flight framing leaned toward a promotion sub-step, not a standalone stage; `requirement_intelligence/run_state/stages.py`'s own `STAGE_DEFINITIONS`, Accepted under ADR-0036 before this task began, already reserves `stage_id="suite_quality_governance"`/`stage_number=16`/`layer="L4"`, distinct from stage 15's own `L3` record — the ADR wins: CP5-wrapping is wired as a real run-state stage (`suite_quality_governance/stage/runner.py::execute_suite_quality_governance_stage`, the same start_stage/should_skip/fail_stage/succeed_stage idiom stages 14/15 already use), not folded anonymously into stage 15's own record, so the already-reserved slot's `governing_citation` moves from "none yet" to a real, live implementation. The two framings are not actually in tension on the one question ADR-0046 D4 itself binds (WHEN CP5 runs, relative to staging) — D4 says nothing about which run-state record owns the bookkeeping. **New wiring, one genuinely new derivation, zero new CP5 logic:** ADR-0046 D2/D7's own text ("D2's orphan check reuses `derive_feature_step_needs`/`derive_unique_step_needs` unchanged, pointed at the whole tracked feature corpus rather than one run's new features") is built literally — `suite_quality_governance/stage/runner.py::_derive_whole_corpus_step_needs` globs every `.feature` file under `baseline_root / FEATURES_SUBPATH` and reuses both functions verbatim; `_read_staged_relative_paths` reads stage 15's own persisted `promotion_report.json` and strips its `baseline_root`-relative `src/test/java/` prefix down to the `JAVA_SOURCE_SUBPATH`-relative shape `CompileAttribution` actually expects (`Promoted.candidate.relative_path`'s own shape) — a real shape mismatch this wiring found and resolved, not merely an assumption. **Off by default:** a new `--with-suite-quality-governance` flag (`scripts/run_requirement_analysis.py`), mirroring `--with-automation-engineering`'s own posture exactly — real live dependencies (a JDK/Maven toolchain via `LiveCompileChecker`, `GeminiEmbeddingProvider`), nested inside stage 15's own gate (has no effect without `--with-automation-engineering`, reported via an explicit console note when set alone, since CP5 wraps THIS run's own promotion and there is nothing to wrap if stage 15 never runs this invocation). **Honest, expected live consequence, not a bug:** `suite_quality_governance/README.md`'s own recorded finding — the real tracked baseline does not currently `mvn test-compile` clean (34 step-definition classes reference page-object classes that do not yet exist) — means a live CP5 run today genuinely FAILS the `compiles` criterion, and therefore CP5's own `overall_verdict`, until that pre-existing gap is separately closed; `CompileAttribution` correctly reports this as pre-existing, not caused by whatever this run staged. Proven with stub seams only (`StubCompileChecker`, a local `_StubEmbeddingProvider`/`_RaisingEmbeddingProvider`), against a REAL per-test git repository (mirroring `test_automation_engineering_promotion_loop.py`'s own "loop-closing proof" discipline): the wrap runs at the right point and sees the assembled baseline-plus-staged state (both a pre-existing and a freshly-staged file are scanned; only the latter, deliberately unreferenced by any feature, is orphaned); a suite-reject (cohesion FAIL) never touches git (`git status`/`git log` unchanged, no unstage, no commit) and is persisted to a new `cp5_report.json` plus a markdown report naming "the shared human review queue" (ADR-0045 D3, no new queue, per D4/Recommendation 3); a suite-pass with nothing review-worthy; a near-duplicate cluster surfaced (`has_review_worthy_findings`) without gating `overall_verdict` (ADR-0046 D6); the off-by-default flag proven both ways at the parser level; SUCCEEDED/FAILED (a genuine uncaught exception, not a scripted compile failure — CP5's own cohesion gate already catches and fails-closed on that)/SKIPPED-on-rerun, mirroring stage 15's own three-test run-state proof. Per-asset promotion and every existing stage-15/CP5-component test are unmodified and pass unchanged — this task composed, it did not rebuild. `make test`: 5480 → 5491 (+11: 9 in the new `tests/unit/test_suite_quality_governance_stage.py`, 2 flag-default/opt-in proofs in `test_run_requirement_analysis.py`); `make lint`: clean; mypy (informational): 433 → 432, no rise (`git stash` baseline re-verified directly before and after). **Owner:** `suite_quality_governance/stage/` (new), `scripts/run_requirement_analysis.py`. **Trigger for the next task:** CP7/CP8 (ADR-0046 D8, named and scoped only) remain Layer 4's own open, not-yet-designed control points; closing the real, pre-existing `mvn test-compile` gap (page-object classes never generated by this platform's own current wiring, item 15's/step 4's own recorded scope boundary) is a separate, tracked prerequisite before a live CP5 run can ever reach a genuine suite-pass.

29. **New (2026-08-07), CP8 built and CP7+CP8 wired live into stage 16 — the direct continuation of item 28's own "Trigger for the next task" (CP7/CP8 were "named and scoped only" as of item 28).** `suite_quality_governance/cp8/` (new package, `c9203cc`, "CP8 Build"): `readiness.py::evaluate_cp8` composes `assets.py` (catalog-reused asset presence), `pom_validation.py` (`pom.xml` validated against its OWN declarations via namespace-wildcard `ElementTree` lookups — `root.findall(f".//{{*}}{{tag}}")` — never a hardcoded dependency list), `junit_platform_config.py` (`junit-platform.properties`'s `cucumber.glue` pointing at a real package), and `glue_resolution.py` (the tracked runner class present, reusing `automation_engineering.catalog.scanner`'s own path-to-package derivation — promoted from private `_package_from_relative_path` to public `package_from_relative_path` for this second consumer, mirroring the `_cosine_similarity`/`method_shape_fits` promotion precedent) into one deterministic, unconditional-on-compile-state gate, per ADR-0047 D7-D9 — distinct from and never merged into CP5-cohesion's own compile check (item 28). `suite_quality_governance/stage/runner.py` (`5d9241f`, "CP& and CP8 wiring" — commit message typo for CP7) then composes CP7 (report-only whole-suite Sonar, §3's 2026-08-06 "CP7 BUILT" bullet) and CP8 (this item, gating) alongside the already-wired CP5 (item 28) into stage 16's own single run: CP8 gates unconditionally; CP7 degrades to an honest `Cp7ReportOutcome(unavailable_reason=...)` on either `SonarScanError` or `httpx.HTTPError` rather than crashing the stage or faking a clean report (`suite_quality_governance/stage/runner.py::_fetch_cp7_report_outcome`); stage 16's own overall verdict composes CP5 and CP8 (both gate), CP7 stays report-only, per ADR-0047 D3's own then-still-open rating-gate-floor TBD (resolved separately by item 34 below). Proven with stub seams (`tests/unit/test_suite_quality_governance_cp8_assets.py`, `..._glue_resolution.py`, `..._junit_platform_config.py`, `..._pom_validation.py`, `..._readiness.py`, `tests/unit/test_suite_quality_governance_stage_cp7_cp8.py`). **Owner:** `suite_quality_governance/cp8/`, `suite_quality_governance/stage/runner.py`. **Trigger for the next task, at the time:** the CP7 rating-gate floor itself (ADR-0047's own open decision) — resolved by item 34; the `mvn test-compile` gap named at item 28 remained open — resolved by item 30.

30. **New (2026-08-07 to 2026-08-09), the page-object gap closure arc — item 28's own named prerequisite CLOSED: the tracked baseline `mvn test-compile`s, CP5-cohesion FAIL→PASS.** A five-commit build arc, recorded coherently rather than per micro-commit: (1) **Page-object request derivation** (`333edde`, `automation_engineering/generation/page_object_reference_derivation.py`, new, 385 lines) derives page-object method needs from the GENERATED step-definition body (not raw Gherkin text, per ADR-0044 D4) — the seam that first made "which page-object method does this step need" a real, computed value rather than an unmet obligation. (2) **Seam extension** (`198e279`) wires that derivation into `live_page_object_generator.py`. (3) **Prompt extension** (`828b9fb`) and (4) **the single-method path corrected to convey the derived method name** (`87ca897`) close **defect 1** (`generate_page_objects` v1.0.0 never conveyed `method_name`, causing ~67% name mismatch on live regen — the finding recorded in the now-superseded live-regen-findings review) by always routing through v1.1.0-equivalent behavior. (5) **Defect 2** (`a85662e`, `generate_step_definitions_v1.1.0.txt`): generated step-definitions switched from `new XPage()` no-arg construction to the real `DriverFactory`-injected constructor every tracked page object actually uses. (6) **Defect 3** (`a7247d3`, `generate_page_objects_v1.2.0.txt`): page objects stopped calling fictional `BasePage` helper methods, generating against the real, introspected `BasePage` method inventory instead. (7) **Defect 4** (`c28ce34`, `generate_page_objects_v1.3.0.txt` + `page_object_reference_derivation.py` extended): the derivation gained `return_type`/`parameters`, not just `method_name`. (8) **Defect 5, generate-side only** (`dd8b08a`, "fix for arity wiring"): the derived request now conveys the step's own required-capture arity into page-object generation — the BIND-side of this same defect (whether a bound/reused method's own signature is checked against `need.captures`) remains open, registered as latent item 36 below. (9) **The step-def model-scoping mechanism** (`7d643ea`, "model adoption change for step definitions") introduced `STEP_DEF_GEMINI_MODEL` as `LiveStepDefinitionGenerator`'s own dedicated provider config (default at the time: `gemini-2.5-flash`), separate from the platform's other Gemini callers — enabling the live regen below; its own default was corrected two commits later (item 32). (10) **The compile-gap closure itself** (`4db1ea2`, "Close the compile gap: live-regenerate all 33 broken step-defs + page objects"): a real-corpus re-run (not a reconstruction) of the 33 needs the tracked baseline's catalog carried as compile-broken, using the fixed chain above plus a non-lite fallback for `MALFORMED_RESPONSE` needs (item 31); found and fixed live, script-side, honestly reported: `gemini-2.5-flash` showed a much higher defect rate than expected at this scale (wrong Cucumber import package, markdown-fenced output, a fabricated duplicate page-object class) and those needs were discarded and regenerated on the lite model instead; two independently-generated needs collided on the same step-def class name (`LoginSteps`) and were merged into one class carrying both methods rather than silently overwriting one (the class-name-collision gap closed properly two commits later, item 33 — this run's own merge was ad hoc, not yet the general `merge_java_classes` mechanism). **Result, verified**: `mvn clean test-compile` — exit 0, BUILD SUCCESS, 71 source files, verified against a genuinely deleted `target/`, twice. CP5-cohesion: FAIL → PASS on the full assembled baseline (compiles + no ambiguous glue) — item 28's own live-run finding ("the real tracked baseline does not currently `mvn test-compile` clean") is no longer the honest state of the world. **This is the big one — item 28's own named prerequisite, now closed** (item 28's additive update note, immediately below, records this). **Owner:** `automation_engineering/generation/`, `automation_engineering/prompts/`, `test-suite-baseline/` (the regenerated Java). **Trigger for the next task:** a live CP5/CP7/CP8 run against the now-compiling baseline, to see whether stage 16's own gates pass for real (not exercised by this arc, which stopped at `mvn test-compile`, never invoking stage 16 itself).

31. **New (2026-08-08), the `MALFORMED_RESPONSE` crash fix — graceful per-need escalation, not a stage crash.** `fb7159b`. `requirement_intelligence/llm/providers/gemini_provider.py`'s `GeminiProvider._execute` previously let a `MALFORMED_RESPONSE`/empty-response outcome from the live Gemini SDK propagate as an unhandled crash; this fix converts it into the same normalized provider-generation-error path `TransportFailureError`'s own family already establishes (item 16's F1 fix, extended), so a single malformed response escalates the one need it belongs to rather than aborting the run. Directly enabled the compile-gap arc's own live regen (item 30) — the coverage probe referenced by this platform's own memory record (non-lite models and `thinking_config` both avoid triggering it; this fix is the honest-failure backstop for whichever model does trigger it) is the same investigation that motivated `7d643ea`'s scoped non-lite model. `make lint`: clean. **Owner:** `requirement_intelligence/llm/providers/gemini_provider.py`. **Trigger for the next task:** none outstanding — the escalation path is exercised live by item 30's own regen run.

32. **New (2026-08-09), step-def model-default correction: `gemini-2.5-flash` → `gemini-3.5-flash`.** `900786b`. Item 30's own compile-gap-closing real-corpus re-run measured `gemini-2.5-flash`'s OUTPUT QUALITY (not just its `MALFORMED_RESPONSE` coverage) at scale for the first time: 17 live generations, only ~24% clean — the rest carried a wrong Cucumber import package, a markdown fence despite the prompt forbidding one, or a fabricated duplicate page-object class. Zero of those defects appeared across 35 combined `gemini-3.1-flash-lite`/`gemini-3.5-flash` generations in the same run. `STEP_DEF_GEMINI_MODEL`'s own default (introduced by item 30's `7d643ea`) is corrected to `gemini-3.5-flash` — a single default-value swap, not a lite-primary/fallback split (judged unproven-necessary at this point, no fallback mechanism built). **Owner:** `scripts/run_requirement_analysis.py`, `.env.example`. **Trigger for the next task:** none outstanding for this correction itself; a future model-quality regression would re-open it.

33. **New (2026-08-10), the class-name-collision coordination gap fixed: detect + merge, escalate on unsafe.** `5f5c6ce`. Item 30's own compile-gap regen surfaced an ad hoc same-name collision (two independently-generated needs both naming their class `LoginSteps`) resolved by hand at the time; this task builds the general mechanism. `automation_engineering/generation/class_collision.py` (new, 212 lines): `merge_java_classes(existing_source, incoming_source)` merges two same-named step-definition classes deterministically when safe — both parse to exactly one top-level class sharing name/package/superclass, and no member (field or method) present in both declares a DIFFERENT body under the same identity (an identical member on both sides is treated as already-merged, not a conflict); any other member is unioned in and the merged source is re-parsed to confirm it is still valid Java before being accepted. Any of these conditions failing raises `UnsafeClassMergeError` — the caller escalates rather than silently picking a winner or overwriting. Wired into `automation_engineering/stage/runner.py`'s own write path, at the point stage 15 would otherwise overwrite one colliding candidate with another. **Honest boundary, not extended by this task:** `automation_engineering/promotion/identity.py::resolve_candidate_identity` still resolves exactly one `CatalogAsset` per candidate (its own docstring: raises if a source resolves to "more than one — e.g. a step-definition class with more than one annotated method, which the generation seam's own contract never produces") — a genuinely merged, now-multi-method class breaks that one-asset-per-candidate assumption, so a merged class only partially promotes today. Registered as open latent item 35 below, not built here. `make test`: +16 test functions (`tests/unit/test_automation_engineering_generation_class_collision.py`, `tests/unit/test_automation_engineering_stage.py`); `make lint`: clean. **Owner:** `automation_engineering/generation/class_collision.py`, `automation_engineering/stage/runner.py`. **Trigger for the next task:** promotion's multi-method extension (item 35) — a dedicated design-and-build task, mirroring how item 26's per-asset promotion gap was resolved.

34. **New (2026-08-10), CP7 rating-gating implemented: report-only → mixed (reliability/`sqale_rating` gate, security/coverage stay report-only).** `657ed5a`. Resolves ADR-0047's own open "exact CP7 rating-gate floor" decision, now that item 30 closed its own stated trigger; `suite_quality_governance/cp7/rating_gate.py`'s own module docstring states the basis directly: "the deferred trigger is met (suite compiles + real, calibratable scores)." `suite_quality_governance/cp7/rating_gate.py` (new, 105 lines): a new `Cp7RatingGateResult` type, separate from the pre-existing report-only `Cp7WholeSuiteQualityReport` (that type still carries no verdict field, unchanged) — gates `reliability_rating`/`sqale_rating` at A-or-B (`1.0`/`2.0`), the floor ADR-0047 D3's own "the suite's own current, real, clean measurement" language names. An unmeasured rating (no value returned by the server) is `WARN`, never `FAIL` — reusing CP1's own pre-existing FAIL>WARN>PASS composition rule rather than inventing a second severity scheme. Composed into stage 16 (`suite_quality_governance/stage/runner.py`, `suite_quality_governance/stage/models.py`): CP7 now contributes to stage 16's own overall verdict for these two ratings only; security and coverage/duplication remain report-only, per ADR-0047 D4/D5's own still-open, explicitly-deferred decisions (the coverage/duplication deferral itself is tracked as latent item 39 below, alongside a documentation-accuracy note about `duplicated_lines_density`). ADR-0047 amended additively (D3's own rating-gate-floor TBD marked resolved; `docs/adr/0047-layer-4-cp7-cp8-freeze.md`, +34 lines) — no existing Decision text edited. `make test`: 5712 → 5728 (+16, verified directly by this consolidation task via checkout — `git checkout 5f5c6ce` before this commit: 5712 passed; `main`/`657ed5a`: 5728 passed); `make lint`: clean. **Owner:** `suite_quality_governance/cp7/rating_gate.py`, `suite_quality_governance/stage/`. **Trigger for the next task:** none outstanding for reliability/sqale; the JaCoCo-coverage prerequisite (latent item 39) and any future explicit security-gating decision remain their own separate, tracked triggers.

    **Additive update to item 28 (2026-08-10, this consolidation task).** Item 28's own two named triggers are both now closed, by the items above: the `mvn test-compile` gap ("closing the real, pre-existing `mvn test-compile` gap... is a separate, tracked prerequisite before a live CP5 run can ever reach a genuine suite-pass") is **CLOSED by item 30** (`4db1ea2`, 2026-08-09) — the tracked baseline now compiles, CP5-cohesion FAIL→PASS, confirmed directly against the current tree (`mvn clean test-compile` was not independently re-run by this documentation-only task, per its own no-code-no-build scope; item 30's own verified record is the evidentiary basis). CP7/CP8, named at item 28 as "Layer 4's own open, not-yet-designed control points," were in fact already designed by that same date (§3's 2026-08-06 ADR-0047/CP7-build bullets, predating item 28's own text slightly) and are now **built and wired live by item 29** (`c9203cc`/`5d9241f`, 2026-08-07), with CP7's own rating-gate floor subsequently resolved by item 34 (`657ed5a`, 2026-08-10). No text of item 28 itself is edited — this note is additive only, mirroring the amendment convention items 21/23/26 already use within this same section.

## 4a. Open latents registered by this consolidation (2026-08-10)

Six engineering-level gaps verified directly against the current code during this consolidation task (distinct from — and at a finer grain than — the ADR-level TBDs items 1-14 track). Each was previously known only from in-session review notes, never entered into this register; each is registered here as a genuinely OPEN item, not resolved by this documentation-only task.

35. **OPEN — promotion's multi-method extension.** `automation_engineering/promotion/identity.py::resolve_candidate_identity` resolves exactly one `CatalogAsset` per candidate (its own docstring names the failure mode: "more than one — e.g. a step-definition class with more than one annotated method, which the generation seam's own contract never produces"); item 33's `merge_java_classes` now legitimately produces exactly that shape for a class-name collision, so a merged, multi-method class only partially promotes today (verified: no caller adapts `resolve_candidate_identity`'s one-asset assumption for a merged source). Design-carrying — touches ADR-0045's own per-candidate promotion contract. **Owner:** `automation_engineering/promotion/identity.py`, `automation_engineering/promotion/gate.py`. **Trigger to resolve:** a dedicated design-and-build task, per item 33's own note, mirroring how item 26's per-run-vs-per-asset promotion gap was resolved.

36. **OPEN — reuse-bind captures-arity, the BIND-side of item 30's defect 5.** `automation_engineering/generation/method_fit.py:115` (`verify_specific_method_fit`) checks `method_shape_fits(method, need.captures)` only on the PRECISE reuse-fit path for a NO_MATCH-adjacent page-object/utility request; item 30's `dd8b08a` fixed arity conveyance on the GENERATE side (the derived page-object request now carries the step's own required-capture count) but a step-definition BIND against an EXISTING catalogued asset does not independently re-verify that asset's own captures against `need.captures` at bind time. **Owner:** `automation_engineering/reuse/engine.py`, `automation_engineering/generation/method_fit.py`. **Trigger to resolve:** a future live run producing a real arity mismatch on a BOUND (not generated) step, or a dedicated audit task, whichever comes first.

37. **OPEN, dormant — Live Page-Object `SemanticMatcher` hardcoded to `step_definitions`.** `automation_engineering/reuse/live_matcher.py` (lines 54, 101, 105, 109) reads/embeds `catalog.step_definitions` unconditionally — page-object and utility reuse decisions have no live semantic-match path of their own today. Dormant, not yet a live defect: per item 20's own recorded scope boundary, page-object/utility generation only ever fires as a NESTED side effect of step-definition generation (`page_object_request`/`utility_request`), so no caller has ever driven a page-object need through `decide_reuse` independently to exercise this gap. **Owner:** `automation_engineering/reuse/live_matcher.py`. **Trigger to resolve:** blocked behind a future page-object/utility-specific reuse-decision entry point being built at all — not scheduled by any item recorded in this document.

38. **OPEN — CP3's Sonar gate catches `SonarScanError` only, not `httpx.HTTPError`.** `automation_engineering/cp3/gate.py:125`'s `_evaluate_sonar_gate` catches only `SonarScanError`; `automation_engineering/cp3/sonar/live_adapter.py`'s `poll_for_completion` (line 172) and `fetch_quality_gate_result` (line 193) both call `response.raise_for_status()` unguarded — a genuine `httpx.HTTPStatusError`/connection failure during either call propagates un-wrapped past CP3's own try/except, crashing the stage instead of failing CP3 closed. The fix pattern already exists one component over: `suite_quality_governance/stage/runner.py::_fetch_cp7_report_outcome` (item 29) catches `except (SonarScanError, httpx.HTTPError)` at the exact same kind of boundary. **Owner:** `automation_engineering/cp3/gate.py`. **Trigger to resolve:** a small, near-term fix mirroring `_fetch_cp7_report_outcome`'s own catch clause — no design task required, the pattern is already proven live elsewhere in this codebase.

39. **OPEN — JaCoCo coverage-report submission, CP7's own D5/D11 prerequisite; plus a `duplicated_lines_density` documentation-accuracy note.** ADR-0047 D5/D11 (`docs/adr/0047-layer-4-cp7-cp8-freeze.md`) locks `coverage`/`duplicated_lines_density` as report-only, unmeasured on the real server today, pending "the JaCoCo-coverage-report-submission build task" (D11) — not built by any item in this document. **Documentation-accuracy note, not built here:** ADR-0047 D10 (2026-08-06) records `duplicated_lines_density` as unmeasured ("neither returned a value for this project"); a later, undated live Sonar re-scan against the now-compiling baseline (item 30) found `duplicated_lines_density` DOES now return a value (`0.0%`) — contradicting D10's own "absent" framing. D10's text is not edited by this consolidation (documentation-only, no ADR text touched); a future task should add an additive correction note to D10 itself, the same convention items 21/22/23 already use elsewhere in this register. **Owner:** the CP3 Sonar adapter / CP7 (`fetch_measures`'s own metric set), ADR-0047 D10/D11. **Trigger to resolve:** the JaCoCo-wiring build task itself (D11) for `coverage`; a small, standalone additive-note task for the D10 correction, independent of D11.

40. **OPEN by design — CP4 evaluates vacuously; no per-asset page-object decomposition exists.** `automation_engineering/cp4/gate.py::evaluate_cp4(page_objects: tuple[Cp4PageObjectInput, ...])` correctly returns `PASS` on an empty `page_objects` tuple — not a bug, the same documented boundary item 20's own resolution note already named ("CP4 therefore evaluates vacuously... until a future task builds that per-step binding-request derivation"). Blocked behind the same root cause as latent item 37: no live caller derives a page-object-specific binding need from raw Gherkin text independently of step-definition generation's own nested `page_object_request`. **Owner:** `automation_engineering/cp4/gate.py`, `automation_engineering/stage/gherkin_needs.py`. **Trigger to resolve:** the same future page-object-specific derivation task item 37 is blocked behind — one build closes both latents.

## 5. How to use this page

- To find out whether a specific decision is locked, find its row above and read its own ADR — this page's summary is not a substitute for the ADR.
- To find out what remains open before a given layer can be built, read that layer's TBD column entries and §4's consolidated list.
- This page is updated whenever a future ADR changes, supersedes, or resolves a TBD item recorded here — by adding a row (new decision), editing a row's Status/TBD columns (a decision changed or a TBD resolved), or adding a note under §3 (a non-ADR state change, like a status-line edit or a register update).
