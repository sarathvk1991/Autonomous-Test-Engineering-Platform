# Governance Dashboard Layer

**Status:** Planned (Phase 7 — not implemented).

Streamlit dashboard surfacing platform metrics, gates, and governance views.

This package is a placeholder reserving the layer's location in the modular
monolith. It will follow the same internal structure as the Requirement
Intelligence Layer (`api/`, `services/`, `models/`, `tests/`, …) and expose a
`router` that is mounted in `app/api/router.py` when the layer is activated.
