"""CP1 engine framework (reserved).

Reserved home for the CP1 engine's reusable, behaviour-free infrastructure — the
criterion contract, registry, and pipeline abstractions that **ADR-0011 §D7**
governs (the ``Criterion → Registry → Pipeline → Aggregate Result`` pattern,
mirroring the Validation Framework).

Intentionally **empty**: this package establishes the subsystem layout only.  No
contract, registry, pipeline, base class, or other implementation exists yet — that
is a later milestone.
"""

from __future__ import annotations
